# Project Memory -- postech-sw-arch-p3

<!-- last-consolidated: 2026-09-03 -->

Add-only log of project-specific learnings. New entries go to the **top** of each section. Never edit historical entries -- add a contradicting entry instead. Consolidacao (agrupar, generalizar, mover originais para `MEMORY.archive.md`) so com aprovacao humana; o marcador `last-consolidated` acima muda apenas nela.

Updated by AI agents at task end per `postech-ai-helper/ai/canonical/task-end-review.md`. Format and update rules: `postech-ai-helper/ai/canonical/memory.md`.

> Entries tagged **[in-flight from PR #N]** describe behaviour that lives on a feature branch not yet merged to `main`. They are kept here so the lesson survives across reviews; once the PR merges, drop the tag.

## Recent decisions

- 2026-09-03 - Repos da fase 3 (p3, lambda, infra-k8s, infra-db, docs) tornados PUBLICOS (decisao do usuario; precedente: p2 ja era publico) apos gitleaks 8.30 no historico completo dos 5 — unicos achados = segredos de DEMO ja publicos desde a fase 2 (k8s/secret.yaml, env.json.example da lambda, JWT_SECRET fixo do conftest de integracao). Consequencias: Actions ilimitado em repo publico (fim do problema de cota/billing); branch protection VIAVEL no plano free -> ATIVADA na `main` dos 5 (PR obrigatorio, 0 aprovacoes, admins incluidos, checks obrigatorios: p3 = lint/type-check/security/test/sbom + 3 jobs do security.yml + ci-plan; lambda = gate/tf-validate; infra = gate; docs = so PR). `homolog` segue livre (deploy por push). Supersede a entrada 2026-07-11 'Branch protection INDISPONIVEL' (Gotchas) e o Adendo (a) do ADR-033 — Adendo (e) registra. Cota do Actions ja tinha renovado em 2026-08-01 (Desbloqueio 2 executado: runs verdes nos 4 repos, CD main+homolog no p3), mas a documentacao seguia dizendo 'cota esgotada' ate 2026-09-03 — refresh de entrega/README/scan-fase-3/RFC-003/roteiro nesta data.
- 2026-09-03 - CONSOLIDACAO (heranca p2, 2026-04..07; originais verbatim em `MEMORY.archive.md`): a base do p3 e o p2 fechado — fase 2 implementada em ADR-015..025/RFC-002 (Clean Architecture sem rewrite, kind unico via Terraform, PostgreSQL StatefulSet, EmailPort+Mailpit, CD por push com GHCR tag por SHA + `kind load`, OTel opcional) e RF-020..024/RNF-017..024. Regras que ficam: (1) LOCK PESSIMISTA nas transicoes de OS e na reserva de estoque (`obter_por_id(com_lock=True)` -> `with_for_update`), ordem global OS -> Estoque, multi-item ordenado por id; `with_for_update` numa instancia JA carregada exige `populate_existing=True` E listener de `refresh` que reidrate os VOs, senao o estado fica stale (#82/#83/#117). (2) Erros de TRANSPORTE no handler de e-mail PROPAGAM (so `OSError`/`SMTPException`) para o relay fazer retry/backoff/DLQ; teste de relay precisa de handler+adapter REAIS contra SMTP morto. (3) SEGURANCA: `papel` obrigatorio no registro (nunca ADMIN por default, #84), logout revoga access E refresh (#118), `ValueError` de dominio -> 422 via handler global (#83 p1), PII mascarada tambem em tracebacks e logs stdlib/uvicorn (`format_exc_info` ANTES de `scrub_pii`; root logger com `ProcessorFormatter`, #86). (4) ORCAMENTO complementar: escopo aprovado congelado em JSONB (`escopo_aprovado_json`, migracao 007); rejeicao restaura orcamento/itens e libera reservas na mesma UoW (#111/#122). (5) RUNTIME: Python 3.14 + NiceGUI 3 + uv; `.python-version`, builder e runtime sobem JUNTOS; `ruff target-version` fica em py313 de proposito; dependabot MENSAL com grupo por ecossistema; par otel pinado (SDK 1.42.x <-> instrumentation 0.63bx) fora do dependabot. (6) ENTREGA: PDF via `scripts/build-entrega-pdf.sh` (capa ABNT, strip da secao de pendencias, anexos A/B/C, links reescritos POR arquivo, mermaid -> PNG); apendice de extras com ADR/PR/commit por item; screenshots e link do video sao residuo manual.
- 2026-09-03 - pip REMOVIDO das imagens de runtime (`Dockerfile` e `ui/Dockerfile`: `RUN python -m pip uninstall -y pip` antes do `USER`): app e UI rodam so pelo venv do uv (/app/.venv) e nunca instalam nada em execucao, entao o pip da base `python:3.14-slim` e superficie morta -- e o trivy do CI (v0.72) audita `pip/_vendor/vendor.txt`, que trazia msgpack 1.1.2 + setuptools 70.3.0 (HIGH) sem nenhum fix possivel pelo lock. Junto: cryptography 49.0.0 -> 50.0.1 (`uv lock --upgrade-package cryptography`, PYSEC-2026-3552 / CVE-2026-69247, o unico achado do pip-audit) -- mesmo bump do PR #13 do dependabot, que rebaseia sozinho. Evidencia: trivy local 0 HIGH/CRITICAL nas duas imagens rebuildadas, `python -m pip` ausente, venvs importam, `make check` verde.
- 2026-08-01 - claude-code-review.yml REMOVIDO (decisao do usuario, "por enquanto"): o repo p3 nunca teve o secret CLAUDE_CODE_OAUTH_TOKEN (era per-repo no p2, nao veio no bootstrap) e a action falhava validacao de env em TODO PR; falhava tambem em PR de dependabot (ator bot rejeitado). Restaurar do git history + `gh secret set CLAUDE_CODE_OAUTH_TOKEN` quando quiser reativar — e ao restaurar, incluir skip de bots (`github.actor != 'dependabot[bot]'`). claude-on-demand.yml FICOU (so dispara manual/comentario; falha sem o secret, mas nao polui PR). Par otel entrou no `ignore` do dependabot.yml: pinado de proposito (SDK 1.42.x <-> instrumentation 0.63bx), bump e manual em par, e a tentativa mensal do bot so gerava run vermelho de resolucao.
- 2026-07-11 - Fechamento local executado (plano v2 no p3-docs): demo SAM integrada rodada de ponta a ponta pela 1a vez (authorizer emulado: 401/403/handler), newman 28 assertions 0 falhas (pasta '00 - Smoke fase 3' na collection), ZAP baseline FAIL 0/WARN 0 (58 URLs), CodeQL 0 ativos (1 constante morta removida; 2 falsos positivos py/unused-global-variable suprimidos — global declarado na l.98 de ui/paginas/login.py). RN-021 provado discriminante: token da lambda na API → 403 'Papel nao autorizado'; adulterado → 401 'Token invalido'.
- 2026-07-11 - Adendo no ADR-033 (limitacoes constatadas na implementacao dos pipelines): (a) branch protection INVIAVEL na org atual (free + repos privados, HTTP 403 "Upgrade to GitHub Pro" nos 4 repos; mesmo limite existe no p2 desde a fase 2 — precedente aceito) → mitigacao = PR obrigatorio por convencao documentada; opcoes se a banca exigir a protecao tecnica: upgrade da org p/ Team ou repos publicos na entrega. (b) homolog nos repos de infra = `terraform plan` no push em homolog, apply automatico so na main (um unico Learner Lab + budget minimo nao comportam infra duplicada; app e lambda tem homolog real via overlay/workspace-stage). (c) ordem de deploy CORRIGIDA: infra-db → infra-k8s → app (p3) → lambda/gateway — a rota HTTP_PROXY do gateway exige a URL publica do app no EKS, que so existe apos o deploy do app (substitui a ordem antiga do ADR-033/RFC-003 §6). (d) segredos de runtime (JWT_SECRET, ENCRYPTION_KEY, ADMIN_PASSWORD, DATABASE_URL) via GitHub Secrets → TF_VAR_*/`kubectl create secret` nos pipelines; Secrets Manager/SSM descartados pelo IAM/KMS do Learner Lab (ADR-026). Reflexos no repo: cd.yml (deploy-eks) cria pytstop-secrets dos GitHub Secrets APP_JWT_SECRET/APP_ENCRYPTION_KEY/APP_ADMIN_PASSWORD e o k8s/secret.yaml de DEMO saiu do overlay EKS (mante-lo la faria o apply sobrescrever o secret real de volta pra demo); prometheus.yaml raspa a API POR POD (kubernetes_sd role: pod + keep app=pytstop-api porta 8000) porque counters OTel sao por processo — sob HPA o scrape via Service balanceia e subconta (dashboards agregam com sum()/sum by()).
- 2026-07-11 - Metricas Prometheus NA API (ADR-032, RF-027/RNF-028): `src/compartilhado/infraestrutura/metrics.py` espelha `relay/metrics.py` (MeterProvider OTel + PrometheusMetricReader no REGISTRY default do prometheus_client), mas expoe via `make_asgi_app()` montado em `/metrics` no proprio FastAPI (API tem ASGI; `start_http_server` e so para o relay daemon). Gate: `API_METRICS_ENABLED` (default OFF, mesmo padrao de OTEL_ENABLED/RELAY_METRICS_ENABLED; demo liga via env). Nomes-CONTRATO com os dashboards: `http_request_duration_seconds{method,rota,status}` (criado como `http_request_duration` + unit="s" — o exportador anexa a unidade), `pytstop_os_criadas_total`, `pytstop_os_duracao_status_segundos{status}` (SEM unit= para nao virar `_segundos_seconds`; label = status ANTERIOR). Erros de integracao NAO duplicados: `outbox_falha_total`/`outbox_dead_total` do relay (ADR-024) ja cobrem. Metricas de negocio via listener `before_flush` em `src/ordem_servico/infraestrutura/metrics.py` (choke-point: toda criacao/transicao de OS passa pelo flush; dominio/aplicacao livres de observabilidade, lint-arch limpo). Wiring no `criar_app` (nao no lifespan: middleware nao pode entrar com o app servindo).
- 2026-07-11 - X-Request-ID externo aceito na borda (RNF-029/ADR-032, cadeia gateway->lambda->app): `SecurityHeadersMiddleware` valida o header (1..128 chars, charset `[A-Za-z0-9._-]`) e propaga; invalido/ausente -> uuid4 novo (anti log/header injection: lixo nunca e ecoado).
- 2026-07-11 - Bootstrap da fase 3: este repo passa a ser a APLICACAO PRINCIPAL do tech challenge fase 3 (1 dos 4 repos exigidos). Base = snapshot da tree do p2 main @ de4d0c6 SEM heranca de historico (decisao da spec de bootstrap). Repos irmaos: postech-sw-arch-p3-lambda (function auth CPF->JWT), -infra-k8s (Terraform EKS), -infra-db (Terraform RDS), -docs (processo: spec, planos, fichamentos, runbook AWS Academy). Entradas abaixo desta data sao herdadas do p2 (contexto historico).
- 2026-07-11 - Requisitos fase 3: docs/requisitos/fase3/ (desafio + gap analysis; IDs RF-025..027, RNF-025..030, RN-021..022 — fase 2 terminou em RF-024/RNF-024/RN-020). Decisoes: ADRs 026-033 em docs/arquitetura/adr/fase3/ + RFC-003. Cloud = AWS Academy Learner Lab (ADR-026: LabRole fixo, sessoes ~4h, credenciais rotativas, conta AINDA NAO ATIVADA — nada aplicado na nuvem).
- 2026-07-11 - Ambiente Azure da fase 2 removido (commit 947fe17): infra/azure-aks, infra/azure-vm, overlays cloud/vm-k3s, alvos make cloud-aks-*/vm-*. infra/ raiz (kind local) PERMANECE — make k8s-up depende dele. Cota GitHub Actions esgotada: gate local (make check) obrigatorio antes de push ate a cota renovar.

## Discovered conventions

- 2026-09-03 - CONSOLIDACAO (heranca p2; originais em `MEMORY.archive.md`): coverage vive em `.coveragerc` (fail_under 95, `source=src,ui`, omit `ui/paginas/*`, `ui/componentes/*`, `ui/app.py`, `ui/__main__.py` — cobertos por E2E), NAO no pyproject; identificadores sem acento (ADR-009), prosa com ortografia normal; Makefile usa `bash -c` (Windows = Git Bash, `MSYS_NO_PATHCONV=1` onde passa path Unix a binario nativo); TESTE DE MESA = suite completa `uv run --no-sync pytest tests/unitarios -q` SEM `-m "not lento"` (o CI filtra os lentos; regressao neles so aparece local) + gates rapidos no HEAD final; thread de bot no PR: responder (accept+SHA / reject+motivo) e RESOLVER via GraphQL, thread humana fica para o humano; NiceGUI: overlay criado em handler de `ui.menu_item` precisa de `with context.client.layout:` (helper `confirmar` ja faz) e `dialog.on("hide", dialog.delete)`; breadcrumb `> [↑ Raiz do projeto] · [↑ Pai]` no topo e no fim de todo doc aninhado (`scripts/lint-doc-anchors.sh`).

## Gotchas

- 2026-09-03 - CONSOLIDACAO (heranca p2; originais em `MEMORY.archive.md`): (1) DOCKER no Mac/colima: `DOCKER_HOST=unix://$HOME/.colima/default/docker.sock` + `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock` (o alvo `test-integ` ja exporta; pytest direto nao); stack kind completa pede colima 4GiB; `docker compose up` retorna antes do uvicorn atender — sempre healthcheck + `--wait`. (2) LOGGING: `_log.info` de src/ NAO sai no container (root em WARNING) — valide por efeito observavel; `structlog.testing.capture_logs` nao intercepta logger module-level ja cacheado (`cache_logger_on_first_use`) — monkeypatch `_log` do modulo em fixture autouse. (3) SQLALCHEMY: evento `load` so no 1o carregamento; apos `expire`/`refresh` dispara `refresh` (assinatura `target, context, attrs`) — listener de reidratacao em AMBOS; Postgres serializa DELETE (FOR UPDATE) x INSERT com FK (FOR KEY SHARE) — usar `bloquear_*_para_remocao`. (4) NICEGUI: fixture `screen` do NiceGUI 3 corrompe `sys.modules['ui']` (guard autouse `_preservar_sys_modules_ui`); `app.storage.tab` antes do websocket levanta RuntimeError (adapter defensivo); `@ui.page` do main_file registra rotas UMA vez por sessao de teste. (5) RELAY/OUTBOX: claim e LEASE (`proxima_tentativa_em = now()+60s`, status segue pendente) — drenagem 'ate o fim' em teste precisa AVANCAR o relogio alem de lease+backoff; `# nosec BXXX` sozinho na linha (prosa depois vira 'Test in comment'). (6) PYTEST: marker/plugin novo precisa existir em TODOS os extras que rodam pytest (`test`, `full-test`) — `--strict-markers` quebra a COLETA no extra sem o plugin (nightly vermelho 13/07-01/08 sem ninguem ver); `full-test/.env` stale aponta pro cluster errado e passa silencioso. (7) WINDOWS/Git Bash: paths Unix convertidos para binarios nativos (`MSYS_NO_PATHCONV=1`), stdout com CRLF (`tr -d '\r'`), pipe nomeado do Docker em `scripts/docker-check.sh`. (8) GHCR privado exige `docker login` no job de deploy mesmo para imagem recem-publicada; `kubectl apply --dry-run=client` no kubectl 1.36 precisa de cluster acessivel.
- 2026-09-03 - Security (cron semanal) ficou VERMELHO de 2026-08-10 ate 09-03 SEM commit no repo: `python:3.14-slim` e tag movel -- o rebuild da base trocou pip 26.1.2 -> 26.2.1 e o trivy passou a acusar msgpack + setuptools vendorizados no pip (lidos de `pip/_vendor/vendor.txt`; aparecem como `python-pkg` SEM caminho de dist-info e com `PkgPath` null no JSON). Achado 'que nao existe no uv.lock' = olhar o pip da base, nao o venv. Reproducao local: `colima start --memory 4` + `docker build -t pytstop:ci-scan .` + `trivy image --scanners vuln --severity HIGH,CRITICAL --ignore-unfixed --ignorefile .trivyignore -f json pytstop:ci-scan` (a saida em tabela do CI nao mostra o path). Run verde de hoje nao garante a base de amanha: o gate depende do que a tag movel entrega.
- 2026-08-01 - Flake 19/20 do test_skip_locked (issue #160) REINCIDIU mesmo com a drenagem final: claim do relay e LEASE via `proxima_tentativa_em = now()+60s` (status continua 'pendente'), entao linha reivindicada-mas-nao-entregue (blip por-linha engolido pelo except de processar_ciclo, ou falha->backoff) fica INVISIVEL para drenagem com relogio em now(). Fix: drenagem final com relogio adiantado alem de lease+backoff (`_agora() + _LEASE + 5s`). Padrao geral: teste de drenagem "ate o fim" contra o relay precisa avancar o relogio, nao so repetir o ciclo — repetir com o mesmo relogio nao enxerga leases vigentes.
- 2026-08-01 - Extra que roda pytest precisa dos plugins exigidos pela config GLOBAL do pyproject (`timeout = 120` + `--strict-markers`): o `@pytest.mark.timeout` adicionado ao test_plano_full em 2026-07-12 quebrou a COLETA no full-test-ci.yml ("'timeout' not found in markers") porque o job sincroniza so `--extra full-test`, que nao tinha pytest-timeout — nightly vermelho de 13/07 a 01/08 sem ninguem olhar. Ao adicionar plugin/marker novo, conferir TODOS os extras que rodam pytest (test, full-test) e reproduzir com `uv sync --extra <x> --frozen` + `--collect-only` num venv limpo. (a) `full-test/tests/test_parallel_usage.py::test_plano_full` tambem precisa de `@pytest.mark.timeout` — o rate limit de /login (5/min) faz o harness dar backoff ate ~65s e o teto global de 120s do pyproject corta antes (flaky local, nao falha funcional; matriz RBAC completa 180/180). test_plano_ci ja tinha; test_plano_full ficou sem ate 2026-07-12. (b) `full-test/.env` (git-ignored) pode ficar STALE apontando pro cluster errado: uma sessao de demo com kubectl port-forward deixa FULL_TEST_BASE_URL=...:18000 / DATABASE_URL=...:15432; rodar full-test assim PASSA SILENCIOSO contra o app do cluster, nao contra o branch. Sempre conferir full-test/.env contra .env.example (8000/5432) antes de rodar full-test apos alternar entre demo e branch.
- 2026-07-11 - SAM local no colima exige DOCKER_HOST=unix://$HOME/.colima/default/docker.sock; function x86_64 sob qemu no Mac ARM crasha o runtime local intermitente ('bad g in signal handler' → 502) — make build-local usa a arch NATIVA do host (dir separado build/lambda-local; o zip do Terraform continua x86_64); psycopg-binary aarch64 so tem wheel manylinux_2_28+. Corpo nao-UTF8 via sam local da 502 no PROPRIO SAM (crash antes do handler) — na AWS real vira base64 (unit test cobre).
- 2026-07-11 - `make test-integ` local no colima exigia `DOCKER_HOST=unix://$HOME/.colima/default/docker.sock` + `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock` (CI ubuntu nao precisa — /var/run/docker.sock ja existe). O alvo `test-integ` do Makefile agora detecta o socket do colima e exporta as duas vars sozinho (padrao herdado do Makefile da lambda); so exporte manualmente se usar pytest direto.
- 2026-07-11 - Branch protection INDISPONIVEL: org free + repos privados (HTTP 403 'Upgrade to GitHub Pro') — vale para os 4 repos da fase 3 E para o p2 desde a fase 2. Mitigacao: convencao de PR obrigatorio (fluxo canonico) documentada; opcoes reais: upgrade Team ou repos publicos na entrega. Branches homolog criadas nos 4 repos (cd.yml: homolog=homologacao, main=producao).
- 2026-07-11 - Deteccao de transicao de OS sem coluna extra: no `before_flush`, `_status` (atributo puro do dominio) diverge de `_status_valor` (coluna mapeada) exatamente no flush que persiste a transicao — o sync `_status -> _status_valor` roda nos `before_insert/before_update` do mapping, DURANTE o flush, depois do `before_flush`. E a duracao vem da history de `_atualizado_em` (committed vs novo). Cuidado correlato: sqlite devolve `DateTime(timezone=True)` NAIVE (Postgres devolve aware) — listener que subtrai datetimes precisa normalizar tz ou quebra so nos testes.

## Tech debt / TODO

- 2026-07-11 - LOW - Imagens GHCR ainda com prefixo `-p2-` nos manifests base (deployment.yaml/relay.yaml/ui-deployment.yaml/jobs/migration-job.yaml e nos seds do cd.yml/Makefile; o overlay EKS ja renomeia p/ `-p3-` via transformer `images`): rename em passada unica futura — os seds e o transformer casam pelo nome completo, trocar todos os pontos juntos senao o replace silenciosamente nao casa.
- 2026-06-12 - MEDIUM - [PR #22] Traces do FastAPI capturam a query string completa: GET /api/v1/acompanhamento?placa=..&documento=.. poe CPF/placa em claro nos atributos http do span. Risco aceito no escopo I11 (Jaeger da demo e in-memory/efemero, flag default off, ADR-020 escopo minimo). Follow-up se OTel passar da demo: server_request_hook sobrescrevendo url.query/http.target, ou tirar documento da query string do canal publico
- 2026-04-26 - LOW - HTTP probes during initial render of a NiceGUI page should be deferred to a `client.on_connect` handler (or equivalent) so they execute with a client context instead of as side effects of import/render

## Code Quality (CodeQL) — gate pre-PR

Antes de abrir um PR que ALTERA codigo Python, rode `make codeql-quality` e garanta **0 findings ativos** (o script sai != 0 se sobrar). Para cada finding novo: (1) corrija se for bug real; (2) suprima na linha com `# codeql[<regra>]` + razao se for FP pontual; (3) desabilite a regra em `.github/codeql/codeql-config.yml` (`query-filters`) ou ignore o path (`paths-ignore`) se a regra nao fizer sentido pro projeto. Nao abra PR com o gate vermelho. Detalhes: `scripts/codeql_quality.py`, `scripts/README.md`.

## Review lessons

- 2026-07-11 - GAP DE EXECUCAO escapou de 7 revisores + 2 pedidos explicitos de teste: `sam local`/demo integrada documentados mas NUNCA executados (SAM nem instalado). 3 causas: (1) "rodar testes" foi operacionalizado como "rodar suites" — instrucao manual documentada nao entrou na definicao; (2) pendencia reportada por subagente ("SAM CLI nao instalado") ficou em nota de rodape e nao virou task; (3) o Judge derruba finding que re-litiga tradeoff aceito — protegeu a premissa desatualizada do ADR-027/029 ("authorizer sem emulacao local"), quando `sam local start-api` suporta authorizer desde a v1.80. Regras novas: (a) toda instrucao executavel documentada precisa de execucao comprovada >=1x antes de "done"; (b) pendencia de subagente vira task rastreada; (c) premissa FACTUAL de ADR verificavel sem custo deve ser verificada no review deep — desafiar premissas nao e re-litigar tradeoff.
