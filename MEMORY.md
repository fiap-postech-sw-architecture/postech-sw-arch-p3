# Project Memory -- postech-sw-arch-p2

Add-only log of project-specific learnings. New entries go to the **top** of each section. Never edit historical entries -- add a contradicting entry instead.

Updated by AI agents at task end per `postech-ai-helper/ai/canonical/task-end-review.md`. Format and update rules: `postech-ai-helper/ai/canonical/memory.md`.

> Entries tagged **[in-flight from PR #N]** describe behaviour that lives on a feature branch not yet merged to `main`. They are kept here so the lesson survives across reviews; once the PR merges, drop the tag.

## Recent decisions

- 2026-07-02 - [docs/entrega-fase2-fechamento] Fechamento do pacote de entrega da fase 2 (issues #123/#124/#125/#93) espelhando o nota-10 da fase 1. **#124 seguranca**: nova **§5 "Relatorio de Analise de Vulnerabilidades"** no `entrega-fase-2.md` (ausente antes; a fase 1 tinha) + `scan-fase-2.md` reescrito **v2.0** (HEAD final `5404826`, ja em 3.14): bandit `src/`+`relay/` 0/0/0, pip-audit 0, trivy imagem-3.14 0 HIGH, gitleaks 0, CodeQL verde, ZAP 0 FAIL. GOTCHA factual: os JSON `docs/seguranca/*-report.json` sao baseline da fase 1 (nao reescritos) — a evidencia da v2.0 sao os check-runs verdes do CI, nao os JSON; #95/#96 sao issues fechadas pelo **#152**, #118-121 pelo **#142**. **#125 extras**: `apendice-funcionalidades-extras.md` novo, **29 extras** em 8 grupos, cada um com ADR/PR/commit/arquivo. GOTCHA: o apendice fica em `docs/entrega/fase2/` (3 niveis), um a MAIS que o da fase 1 (`docs/entrega/`, 2 niveis) — links relativos p/ a RAIZ (src/ relay/ migrations/ k8s/ scripts/ .github/ Dockerfile) precisam de `../../../`, nao `../../` (que cai em `docs/`); os de `docs/` (arquitetura/ tech-debt/ seguranca/) ficam em `../../`. **#93 roteiro**: bloco 6 ganhou o passo das metricas Prometheus da outbox (`svc/prometheus:9090`, `outbox_entregue_total`/`outbox_pendentes`; ADR-024 = metricas do RELAY, nao da API). **#123 PDF**: `scripts/build-entrega-pdf.sh` (ja existia "seco") virou pipeline completo — capa ABNT pre-pendida (FIAP/15SOAT, 5 RMs, Sao Paulo 2026, `break-after:page`), **strip da §9 Pendencias** (checklist interno, nao vai pro PDF), anexos A (scan)/B (evidencias visuais — checklist+placeholders, precisam captura ao vivo)/C (extras), `rewrite-md-links.py` rodado POR-ARQUIVO (cada base-dir) antes de concatenar, mermaid->PNG, pandoc+weasyprint. Inserir a §5 RENUMEROU §5->§9 (Pendencias virou §9) — atualizei refs "secao N" e o alvo do `awk` (§8->§9). PDF validado end-to-end (pandoc/weasyprint/npx locais): capa+§5+anexos presentes, §9 NAO vaza (296KB). RESIDUO MANUAL (como o video): screenshots do anexo B (HPA/Jaeger/Mailpit/Prometheus/SonarQube) e o `VIDEO-LINK-FASE-2` — o script avisa se o placeholder persiste. PR aberto SEM merge (pedido do usuario).

- 2026-07-02 - [chore/python-3.14] Migração Python 3.13->3.14 (desbloqueada pelo NiceGUI 3 que removeu `vbuild`/`pkgutil.find_loader`). **O TRIO SOBE JUNTO**: `.python-version`=3.14 (uv cria a venv NA VERSAO daqui) + `Dockerfile` builder `ghcr.io/astral-sh/uv:python3.14-bookworm-slim` + runtime `python:3.14-slim` (builder/runtime na MESMA minor senao `/app/.venv/bin/python` vira symlink quebrado — bytecode/wheels sao por-minor) + `ui/Dockerfile` 3.14 + os 7 pins `python-version:"3.14"` nos workflows (ci.yml x5, full-test-ci, security). **DECISAO DE PINS DE TOOLING** (review de 3 agentes: build/CI, compat, release-safety): mypy `python_version=3.14` e `sonar.python.version=3.14` (analisam a stdlib que EMBARCA, zero churn — mypy 167 arquivos verde) MAS **ruff `target-version` MANTIDO em py313**: sob ruff 0.15.20 o py314 vira o formatter (5 arquivos "would reformat") + dispara um TC001 em `schemas.py` — tudo em codigo alheio a migracao de runtime; bump do ruff e cosmetico ("harmless to leave"), rejeitado p/ manter o gate verde e o PR minimo. `requires-python` MANTIDO em `>=3.12` (floor soft; nao mexe no uv.lock, que continua `--frozen`-valido — bumpar forcaria regen do lock). Compat (agente): ZERO uso de API removida no 3.14 no first-party; TODAS as deps tem wheel `cp314 manylinux2014` (greenlet 3.5, grpcio 1.81, pydantic-core 2.46, psycopg2 2.9.12, bcrypt 5.0, cryptography 49) — sem source build. GOTCHA CI: o step de lint do ci.yml cobre `src/ ui/ tests/` (NAO relay/ scripts/ como o `make lint`); o de mypy cobre `src/ ui/`. Docs corrigidos (a migracao TROCA o runtime que eles descreviam): `finalization-plan.md:9` "3.14 deferido" -> "deferido *naquele momento* ... desbloqueado em seguida" + 4 refs "runtime é 3.13" -> "3.14" (trackers do #90, que segue aberto). Gates 3.14: ruff/format/arch(3/3)/mypy(167,py3.14)/bandit limpos, make test 1589 cov 95.31%, test-integ 163 (redis 8 real via testcontainers), CodeQL 0, ambas imagens Docker build+smoke em 3.14.6. TESTE DE MESA no app 3.14.6 vivo (stack compose rebuildada): login 200 (bcrypt5 verify + pyjwt), GET metricas 200 (dados reais, 9 OS), POST cliente 201 -> GET 200 -> POST cpf duplicado 409 (regra de dominio), 401 sem token, 422 payload invalido — stack nativo inteiro servindo trafego autenticado real. Rollback = `git revert` de 11 arquivos de config/doc (sem migracao de dados, sem mudanca de lock). Dev local com venv 3.13 antiga: `uv sync` reconstroi a venv em 3.14.
- 2026-07-02 - [feat/nicegui3-e-rejeicao-complementar] Upgrade NiceGUI 2.24->3.14 + fix #111/#122 num PR so. **NiceGUI 3** (dependabot #146): zero mudanca no codigo da UI (138 testes de logica passam, todos os modulos ui importam) e REMOVEU a dep `vbuild`, DESTRAVANDO o Python 3.14 (o `pkgutil.find_loader` sumiu). Testing: o marker `module_under_test` (plugin nicegui 2) saiu; o fixture `screen` agora roda o app do `main_file` -> `@pytest.mark.nicegui_main_file("ui/__main__.py")` (registra as paginas e chama ui.run() sob __mp_main__). GOTCHA: no nicegui 3 o main_file registra as rotas UMA vez; reabrir a pagina num 2o teste de browser cai em "not found" — consolidei os 2 testes de login num so. Rodar browser (lento) + nao-browser juntos (`pytest tests/unitarios/ui` sem `-m`) polui estado e quebra auth_guard, mas os make targets separam (`make test` -m "not lento" vs `make test-lento` -m lento) — nao e regressao. **#111/#122** (causa raiz: `gerar_orcamento_complementar` SOBRESCREVIA `_orcamento` e `rejeitar` so transicionava -> OS cobrava trabalho recusado): novo snapshot do ESCOPO APROVADO (orcamento + ids dos itens cobertos) congelado nas aprovacoes, persistido na coluna JSONB `escopo_aprovado_json` (migracao 007, reversivel) via helpers `_orcamento_para/de_dict` extraidos (DRY com orcamento_json). `rejeitar_orcamento_complementar` -> `tuple[ItemDaOrdem,...]` (restaura orcamento aprovado, remove itens fora do escopo, retorna-os); o use case (agora com estoque_port) libera as reservas na MESMA UoW, em ordem de id, calculadas ANTES do save que os deleta. `finalizar_servico` (#122) recusa itens fora do escopo aprovado. Ordens legadas (escopo NULL) preservam o comportamento antigo. GOTCHA: `data: Any` nos helpers JSONB dispara ruff ANN401 -> `# noqa: ANN401` (payload JSONB e dinamico, como o inline com type:ignore antes). Testes (TDD): dominio + use case (libera reserva) + mapping round-trip do escopo pela JSONB + migracao 007 ida-e-volta contra Postgres real. TESTE DE MESA no app real (stack rebuildada, nicegui 3 + migracao 007 + redis 8): fluxo #111 end-to-end via API — aprovar (R$150, estoque 15) -> add peca (estoque 12) -> finalizar BLOQUEADO 409 (#122) -> complementar (R$690) -> REJEITAR: orcamento volta a R$150, item removido, estoque 15 -> finalizar 200. UI no browser (playwright): login via atalho Admin, dashboard com dados vivos, lista de OS renderizam no nicegui 3. Gates: make test 1584 verde cov 95.34%, make test-integ 163 verde, lint/format/mypy(180)/bandit/lint-arch(3.0) limpos, CodeQL 0 ativos. GOTCHA op: `make up` NAO rebuilda (usa imagem cacheada) — `docker compose up -d --build app` para pegar migracao/codigo novos.
- 2026-07-02 - [chore/deps-migracao / PR #143] Migracao consolidada de dependencias substituindo os 12 PRs do Dependabot (#130-#141, fechados). `uv lock --upgrade` dentro das constraints subiu **redis 5.3.1→8.0.1** (major; constraint `>=8,<9`; validado pelo teste de integracao real `test_rate_limit_redis.py` com testcontainers), structlog 25.5→26.1, uvicorn 0.44→0.49, sqlalchemy 2.0.49→2.0.51, pydantic floor 2.13.4, testcontainers 4.14, ruff 0.15.20 + minor/patch; GitHub Actions checkout v4→v7, setup-python v5→v6, upload-artifact v4→v7, setup-uv v4→v7, login-action v3→v4. `dependabot.yml` passou a MENSAL + grupo catch-all por ecossistema (`patterns:["*"]`, major incluso) = 1 PR/ecossistema/mes (antes ~12 PRs semanais, cada um com CI+full-test — caro em Actions). GOTCHA CENTRAL (Python 3.14 DEFERIDO, docker #130/#131): (1) `vbuild` (dep transitiva do NiceGUI/UI) usa `pkgutil.find_loader`, **removido no 3.14** → a suite de UI quebra na COLETA com `AttributeError` (nao aparece em unit/integ que nao importam UI — so `make test` cheio pega); (2) `uv` respeita `.python-version` e cria a venv NA VERSAO DELE, entao bumpar so o `FROM python:3.14` do Dockerfile gera venv 3.13 com symlink de python QUEBRADO no runtime 3.14 (`/app/.venv/bin/python` inexistente; a pista foi `coverage-3.13` no venv). Migrar 3.14 exige `.python-version`=3.14 + builder/runtime alinhados + NiceGUI com vbuild compativel — os PRs do dependabot (so `FROM`) eram inmergeaveis. Gates: uv lock --check ok, lint(ruff 0.15.20)/format/lint-arch/mypy(180)/bandit(0 high) limpos, make test 1578 verde cov 95.37%, make test-integ 162 verde (redis 8 real), CodeQL 0 ativos, CI do PR 12/12 (full-test buildou a imagem 3.13 com as deps novas).
- 2026-07-01 - [fix/p0-auditoria-fase2 / PR #142] 4 bugs P0 da auditoria de finalizacao, um PR (TDD, red→green provado por stash+run nos de concorrencia). **#117** (a defesa de #82/#83 era parcial): `select(...).with_for_update()` numa session que JA carregou a instancia devolve o objeto do identity map SEM recarregar colunas — o FOR UPDATE trava no banco mas o estado em memoria fica stale (guard de `DecidirOrcamento` le sem lock, delegado re-le com lock na MESMA session → stale). Fix em DUAS partes, ambas necessarias: (a) `.execution_options(populate_existing=True)` no ramo com_lock de `obter_por_id` (OS+Estoque) e no `obter_por_ids` (lote); (b) GOTCHA CENTRAL — populate_existing e `session.refresh` disparam o evento **`refresh`**, NAO `load`; sem listener de refresh as colunas voltam frescas mas os VOs reidratados (`_status`, `_orcamento`, `_preco_unitario` Dinheiro) ficam stale (mesma licao do cliente_veiculo 2026-04-29). Solucao: unir load+refresh num listener so com `@event.listens_for` empilhado + `def _fn(target, *_args)` (o refresh passa `attrs` extra) — espelha before_insert/before_update; evita C901 dos wrappers separados. Teste `test_concorrencia_lock.py::TestReleituraComLockRefrescaIdentityMap` prova as duas partes (com so o populate_existing, `.status` volta stale). **#120**: `_montar_item` so validava existencia da peca; `ItemEstoqueDTO` ganhou `ativo` (default True p/ nao quebrar consultas so-leitura; adapter popula o real) e `_montar_item`+`ItemEstoque.reservar` rejeitam item inativo (ViolacaoRegraDeNegocio). **#118/#121**: logout so revogava o jti do access (refresh sobrevivia, CWE-613) → agora aceita refresh opcional no corpo (`Body(None, embed=True)`, sem quebrar clientes so-header) e revoga ambos jti, best-effort e escopado ao dono; `revogar` idempotente (guard `esta_revogado` antes do INSERT) fecha o 500 do logout duplo. **#119**: caminho `recusada` de `DecidirOrcamento` re-le com_lock=True e revalida estados de espera antes de delegar a `CancelarOrdem` (que aceita qualquer estado ativo) — fecha o TOCTOU; caminhos aprova↔complementar seguem best-effort (delegado revalida sob lock via maquina de status). Gates: lint/format/lint-arch(3·0)/mypy(180)/bandit(0 high) · `make test` 1578 verde cov 95.37% · `make test-integ` 162 verde · CodeQL 0 ativos. Copilot #142: 2 sugestoes nao-bloqueantes aceitas (comentarios de invariante no re-arm de `_eventos_pendentes` e no roteamento best-effort).
- 2026-07-01 - [docs/finalization-plan-fase-2] Auditoria de finalização da fase 2 em 4 frentes paralelas (rastreabilidade enunciado→código, baseline vs PDF nota-10 da fase 1, caça-bugs nos fluxos críticos, auditoria do ledger/issues/PRs). Resultado: 12 issues novas (#117-#128; #128 = índice do plano), plano operacional em `docs/entrega/fase2/finalization-plan.md` + `docs/entrega/fase2/guia-agente-executor.md` (playbook autossuficiente para agente executor com LLM menor), TD-032/TD-033 no ledger. Achados centrais: (a) `com_lock` sem `populate_existing` devolve instância stale do identity map (#117) — derrota parcial da defesa #82/#83; (b) causa raiz do #111 é `gerar_orcamento_complementar` SOBRESCREVER `self._orcamento` (`ordem_de_servico.py:265-283`) e a rejeição não reverter nada — escopo real inclui itens não removidos + reservas nunca liberadas; (c) logout não revoga refresh (CWE-613, #118); (d) rastreabilidade 10/12 OK — pendências são só cauda de submissão (vídeo, soat-architecture 404, PDF, collection #113 quebraria na banca por HMAC). Issues #83/#35/#33/#31 estavam RESOLVIDAS em main (PRs #101/#50/#64/#62) mas esquecidas abertas — fechadas com evidência em 2026-07-01. PR #55 (autofix Copilot) corrompe 3 arquivos de teste, CI vermelha — fechado em 2026-07-02. PR #116 mergeado em 2026-07-01 (931d6aa) — #75 fechada, gates + upgrades de 5 CVEs na main (alertas Dependabot 39→34). Baseline fase 1: o corpo do doc da fase 2 já supera (rastreabilidade RF→PR→evidência→vídeo); o delta está no PDF (sem capa ABNT, sem anexos de evidência, sem seção de segurança — #123/#124/#125)
- 2026-06-29 - [fix/82-83-concorrencia-lock] Fix #82 + #83 (concorrencia sem lock no caminho de persistencia — bundle, lock pessimista `SELECT ... FOR UPDATE`, sem migracao, espelhando o `com_lock` que Estoque ja tinha). #82 (transicoes de OS sem controle de concorrencia → N eventos/e-mails): `OrdemDeServicoSQLAlchemyRepository.obter_por_id` (`repository.py:78`) ganhou `*, com_lock: bool = False` — `True` carrega via `select(OrdemDeServico).where(id==...).with_for_update(nowait=False).scalars().one_or_none()` (ramo de leitura segue `session.get`). Mesma assinatura no Protocol de dominio (`dominio/repository.py:22`). O helper compartilhado `_obter_ordem` (`aplicacao/use_cases.py:135`) ganhou `*, com_lock: bool = False` e repassa ao repo; os 11 casos de uso de MUTACAO/TRANSICAO passam `com_lock=True` (Adicionar/RemoverItem, IniciarDiagnostico, GerarOrcamento, AprovarOrcamento, FinalizarServico, RegistrarEntrega, CancelarOrdem, Gerar/Aprovar/RejeitarComplementar). CAMINHOS DE LEITURA ficam no default `False` (sem lock): `ObterOrdem.executar` (:783) e o GUARD de `DecidirOrcamento.executar` (:724) — este ultimo so checa o estado de espera; a transicao real (e o FOR UPDATE) acontece no caso de uso delegado, que re-le com_lock=True (1a chamada False=guard, 2a True=delega; provado em `test_use_cases_os.py::TestDecidirOrcamento::test_guard_le_sem_lock_e_delegada_le_com_lock`). GOTCHA do lock-antes-da-UoW: em todos os casos de uso a session e COMPARTILHADA (composition root: `dependencies.py:76` `session_factory=lambda: session`), entao `_obter_ordem(com_lock=True)` ANTES do `with self._uow:` adquire o FOR UPDATE na unica tx lazy da session e o lock e retido ate `uow.commit()` (o `__enter__` da UoW so devolve a mesma session, nao abre tx nova) — nao precisa mover o load pra dentro do bloco. #83 (lost-update na reserva/liberacao → sobre-venda): `EstoqueSQLAlchemyAdapter.reservar`/`liberar` (`adapters.py:41,57`) trocaram o `session.get(ItemEstoque, ...)` cru por um helper `_obter_item_com_lock` que REUSA `ItemEstoqueSQLAlchemyRepository.obter_por_id(com_lock=True)` (unica fonte do `with_for_update` do contexto Estoque — ACL nao reimplementa a query do vizinho; import do repo local no metodo + `ItemEstoque` sob TYPE_CHECKING so pra anotacao-string). ORDEM GLOBAL DE LOCKS definida: OS antes de Estoque (AprovarOrcamento trava a OS no `_obter_ordem` e so depois reserva os itens) — evita deadlock cruzado com a finding de OS. ANTI-DEADLOCK multi-item: novo helper `_reservas_de_estoque_ordenadas(ordem) -> list[tuple[UUID,int]]` (`use_cases.py:154`) filtra itens com `item_estoque_id` (estreita `UUID|None`→`UUID`, resolve o mypy) e ORDENA por `item_estoque_id` (espelha o `order_by(id)` de `obter_por_ids`); AprovarOrcamento (:482) e CancelarOrdem (:595) iteram por ele em vez de `ordem.itens` cru (que estava em ordem de insercao). Testes (TDD, red→green): novo `tests/integracao/ordem_servico/test_concorrencia_lock.py` (Postgres real, 2 conexoes/sessions proprias `engine.connect()` — NUNCA a fixture `session` de savepoint, que mascara FOR UPDATE entre tx). #82 deterministico (modelo do fencing test do relay, sem sleep-como-sync): conexao A carrega a OS com_lock pela MESMA session, faz flush (UPDATE, lock retido, sem commit), thread B chama o use case REAL `IniciarDiagnostico.executar` e DEVE bloquear no FOR UPDATE de `_obter_ordem` (sincronizacao por CONDICAO observada: `_esperar_backend_bloqueado` faz poll em `pg_stat_activity.wait_event_type='Lock'`, nao sleep fixo); A enfileira o evento (helper `_enfileirar_eventos_da_ordem` espelha o que a UoW faz antes do commit — a UoW comita atomicamente, sem ponto de pausa) e comita; B desbloqueia, re-le `em_diagnostico` e a maquina rejeita → 1 conflito + EXATAMENTE 1 `DiagnosticoIniciadoEvent` na outbox (a prova central: 1 evento, nao N). Provado que pega o bug por INVERSAO de mesa (com_lock→False no `_obter_ordem`): B nao bloqueia no read, valida stale e sucede → "B deveria conflitar, obtido sucesso" + 2 eventos. #83: duas reservas concorrentes do mesmo item (saldo 3, cada pede 2) → 1 vence, a outra serializa no FOR UPDATE, re-le saldo=1 e falha com `EstoqueInsuficienteException`; saldo final 1, nunca <0, soma reservada <=3 (inversao p/ `session.get`: ambas sucedem, "esperado 1 sucesso, obtido 2" = sobre-venda). Mais: teste determinista de lock-bloqueia-2a-tx (FOR UPDATE NOWAIT falha enquanto A detem), teste multi-item ordem-de-id sem deadlock, e teste de que o caminho de LEITURA default NAO trava (probe FOR UPDATE NOWAIT sucede com a session de leitura aberta). Seeds COMITAM de verdade (threads usam conexoes proprias) com CPF (`brutils.cpf.generate`) e placa (`LCK%04d` contador) UNICOS por seed + cleanup escopado por id no `finally` — independente da ordem/limpeza dos demais. Unit: fakes do OS repo (`test_use_cases_os.py:80` agora registra `chamadas_obter_por_id:list[tuple[UUID,bool]]`; `test_notificacoes.py:60`) e os adapter tests (`test_adapters.py` — `reservar`/`liberar` agora mockam `session.scalars().one_or_none()` em vez de `session.get`; fixture autouse `_mapeamento_estoque_registrado` chama `iniciar_mapeamentos()` pois `select(ItemEstoque).with_for_update()` exige a classe mapeada — senao so passava por efeito da ordem de coleta). Novo `TestLockPessimistaNasTransicoes` afere o flag com_lock por caso de uso (transicoes True, ObterOrdem False). Gates: `uv lock --check` ok, lint/lint-arch(3 kept 0 broken)/typecheck(177)/security(bandit 0 High)/sbom(48 refs) limpos, `make test` 1544 verde (cov 95.40%), `make test-integ` 149 verde (inclui os 6 novos de concorrencia), `CODEQL_DIR=$HOME/codeql-tools make codeql-quality` 0 ativos. DEADLOCK: ordem global OS→Estoque + locks multi-item em ordem de id fecham as duas janelas; sem risco novo identificado. NAO commitado (a pedido). NOTA achado lateral (fora de escopo, task spawnada): `test_repository_estoque.py::test_listar`/`test_obter_por_ids` ja eram order-dependent (precisam do mapping registrado por outro modulo antes) — falham isolados, passam na suite cheia
- 2026-06-29 - [fix/86-pii-tracebacks] Fix #86 (PII vaza em tracebacks + logs stdlib — gap LGPD): dois defeitos somados, ambos em `src/compartilhado/infraestrutura/logging.py`. (1) ORDEM dos processors — `scrub_pii` rodava ANTES de `format_exc_info`, entao a chave `exception` (traceback montado por format_exc_info) nascia DEPOIS do scrub e nunca era mascarada. Fix: `format_exc_info` agora vem ANTES de `scrub_pii` na cadeia (o traceback e uma STRING que `_scrub_value`/`_mask_string` ja percorrem, entao o reorder basta — `scrub_pii` mascara a chave `exception` sem codigo especial). `StackInfoRenderer` tambem precede o scrub (stack_info vira string mascarada). (2) STDLIB bypass — o handler 500 (`error_handler.py:94` `logger.exception`), o `_value_error_handler` (:82 `exc_info=exc`), `encryption.py:36` (warning) e os access/error logs do uvicorn usam `logging` stdlib e NAO passavam pelo pipeline do structlog. Fix: `configurar_logging` agora instala um `structlog.stdlib.ProcessorFormatter` num `StreamHandler(sys.stdout)` no ROOT logger, com a `_cadeia_compartilhada` (inclui `scrub_pii`) no `foreign_pre_chain` — logs estrangeiros sao scrubados ANTES de renderizar; logs ja-structlog usam `ProcessorFormatter.wrap_for_formatter` como ultimo processor e PULAM o foreign_pre_chain (sem duplo processamento). GOTCHA CENTRAL do uvicorn: lancado por CLI (`uvicorn src.main:app`, entrypoint.sh:32) ele aplica `LOGGING_CONFIG` via dictConfig com `uvicorn`/`uvicorn.access` tendo handler PROPRIO + `propagate=False` — esses logs (access log carrega PII no path/query string, ex.: `?documento=CPF`) NAO chegariam ao root. `configurar_logging` roda no lifespan startup DEPOIS do dictConfig do uvicorn, entao limpa `uvlog.handlers=[]` + `uvlog.propagate=True` para os 3 loggers (`_LOGGERS_UVICORN`), religando-os ao scrubber do root (provado por simulacao da ordem exata: dictConfig→configurar_logging→access log com `documento=123.456.789-00` sai `***.***.789-**`). Isso fecha tambem o caminho de log da TD "PII na query string dos traces" (a TD do OTel/span segue aberta). (3) SCRUBBER ampliado: `_TELEFONE_PATTERN` (telefone BR FORMATADO — exige DDD 2-digitos com/sem parenteses + `\s` + `9?\d{4}-\d{4}`, prefixo `+55` opcional, com lookarounds `(?<!\d)`/`(?!\d)`; o split com HIFEN 4-4/5-4 + DDD e o discriminador contra falso-positivo: precos `1500.00`, anos `2026`, portas `8000`, ids `12345` e ATE cartao `4111-1111-1111-1111` ficam INTACTOS — cartao nao esta no escopo desta issue; `3456-7890` sem DDD tambem nao casa, trade-off deliberado) + denylist `_CHAVES_SENSIVEIS` (frozenset case-insensitive: password/senha/token/secret/authorization/refresh_token/access_token/api_key) que mascara o VALOR inteiro pelo NOME do campo (top-level E aninhado, em `scrub_pii` e `_scrub_value`), nao por regex — cobre segredos sem forma fixa. `_mask_string` aplica CPF→CNPJ→email→telefone nessa ordem (CPF antes elimina digitos que o telefone falso-positivaria). RELAY (`relay/__main__.py`): removido o `logging.basicConfig` (e imports `logging`/`sys`) que existia so pra dar handler INFO ao root — `configurar_logging` agora faz isso ELE PROPRIO (e com scrub); comportamento de visibilidade INFO preservado. `configurar_logging` ganhou param `stream: TextIO | None = None` (default sys.stdout) so pra testes capturarem o output renderizado; substitui `root.handlers` (idempotente em warm restart/teste). Outros sites de excecao stdlib auditados: dispatcher/notificacoes/listener usam `structlog.get_logger` (ja no pipeline correto); so error_handler+encryption+uvicorn eram stdlib e agora estao cobertos pelo ProcessorFormatter do root. Testes (TDD, red→green): `test_logging.py` ganhou TestScrubTelefone (formatado mascarado + 5 nao-telefones preservados), TestScrubChavesSensiveis (8 chaves + case-insensitive + aninhado + nao-sensivel intacto), TestPipelineMascaraTraceback (excecao com CPF+email+telefone via pipeline real structlog E stdlib → `exception` existe e PII mascarada; uvicorn religado ao root com handler cru limpo); `test_error_handler.py` ganhou 2 testes e2e (handler 500 e 422 com PII no traceback → buffer scrubado). GOTCHA dos testes: fixtures que dao `yield` valor NAO podem ter prefixo `_` (ruff PT019 trata como fixture-sem-valor) — nomear `logging_pipeline`/`pipeline_buffer`; e restaurar `root.handlers`/`root.level`/`structlog.get_config()` no teardown porque `configurar_logging` instala handler global. Gates: `uv lock --check` ok, lint/lint-arch(3 kept)/typecheck(177)/security(bandit 0 High/0 Medium)/sbom(48 refs) limpos, `make test` 1539 verde (cov 95.40%), `CODEQL_DIR=$HOME/codeql-tools make codeql-quality` 0 ativos. Verificacao de mesa do caminho de producao (sem stream override → sys.stdout): 500 real via FastAPI emite traceback com `RuntimeError: cliente ***.***.789-** / f***@example.com / ***` (CPF+email+telefone+CNPJ todos mascarados no `exception`)
- 2026-06-29 - [fix/84-registro-papel] Fix #84 (RBAC anulado): `POST /autenticacao/registrar` criava SEMPRE ADMIN porque `Usuario.criar` tinha default `papel=Papel.ADMIN` e o request/DTO nao tinham campo `papel`. Design fail-safe: `papel` agora e OBRIGATORIO em `RegistrarRequest` (`schemas.py`) e `RegistrarDTO` (`dtos.py`); o router (`router.py:61`) propaga `body.papel`; `Registrar.executar` passa `papel=dto.papel`; `Usuario.criar(papel)` perdeu o default. Omitir papel -> 422 (nunca ADMIN silencioso). GOTCHA central: NAO da pra so remover o default do campo `_papel` no dataclass — `Entity.id` tem `default_factory=uuid4` (base), entao um campo sem default DEPOIS dele estoura `TypeError: non-default argument follows default argument` na DEFINICAO da classe. Solucao: `_papel: Papel = field(kw_only=True)` — required keyword-only, fora da ordem posicional; todos os call sites ja passavam `_papel`/`papel` por palavra-chave, entao non-breaking. O `load` event do mapping imperativo reidrata via `target.__dict__["_papel"]` (bypassa `__init__`), entao `kw_only` nao afeta o ORM; a coluna `usuarios.papel` mantem `default="admin"` no SQLAlchemy (defesa em profundidade morta — `before_insert` sempre seta `_papel_valor` do `_papel` explicito). VALORES no JSON sao os do StrEnum em MINUSCULO (`"admin"/"mecanico"/"atendente"`) — `"ADMIN"` e REJEITADO (422); OpenAPI marca `papel` required com enum exposto. Import de `Papel` em `usuario.py` virou TYPE_CHECKING (so anotacao agora; ruff TC001); em `dtos.py` tambem TYPE_CHECKING; em `schemas.py` e RUNTIME (pydantic v2 precisa do tipo p/ montar o validator) — interfaces->dominio permitido (lint-arch 3 kept 0 broken). Testes (TDD): regressao no use case (`test_use_cases_auth.py`: parametrizado por papel + `test_registrar_mecanico_nao_vira_admin` afirmando `is not Papel.ADMIN`), schema (`test_security.py`: `test_papel_obrigatorio` 422, `test_rejects_papel_invalido`, `test_accepts_cada_papel_valido`; `test_rejects_extra_fields` REESCRITO — antes usava `papel="admin"` como campo EXTRA, que agora e valido, trocado por `superuser=True`), router (`test_router_auth.py`: afirma DTO carrega `papel` + `test_registrar_sem_papel_retorna_422` com `executar.assert_not_called`). Tests existentes que chamavam `Usuario.criar`/registravam sem papel atualizados (mecanico, sem enfraquecer assert). Comentario stale em `seed_usuarios.py:6-8` corrigido (endpoint AGORA aceita papel; seed segue direto no banco por ser bootstrap circular — precisa de admin autenticado). UI nao chama `/registrar` (so seed) — sem regressao runtime. Gates: `uv lock --check` ok, lint/lint-arch(3)/typecheck(177)/security(bandit 0 high)/sbom(48 refs) limpos, `make test` 1512 verde (cov 95.37%), `CODEQL_DIR=$HOME/codeql-tools make codeql-quality` 0 ativos
- 2026-06-25 - [feat/td-008-transactional-outbox] Hardening operacional do relay TD-008 (5 itens num so commit): (1) RESILIENCIA por ciclo — o `while` de `executar_relay` (`relay/listener.py`) agora embrulha heartbeat+`_drenar` num `try/except Exception` que loga `outbox_ciclo_falhou` (exc_info) e `continue` (blip transitorio de DB nao reinicia o pod nem desmonta o LISTEN; at-least-once+lease garantem zero perda). ESCOPO: so o drain DENTRO do while; o drain inicial PRE-loop fica fora (um erro no boot deve falhar alto). `except Exception` (nao BaseException) deixa KeyboardInterrupt/SystemExit propagarem. Falha do gauge cai DE PROPOSITO no mesmo except (mesma classe de blip). (2) GAUGE de profundidade (§7) — `emitir_profundidade(engine)` em `processador.py`, chamado 1x por ciclo de drain no fim de `_drenar`; uma query `count(*) FILTER (WHERE status='pendente'/'dead')` + `EXTRACT(EPOCH FROM now()-min(criado_em) FILTER...)`, loga `outbox_profundidade` info com `pendentes`/`idade_mais_antigo_s`(None quando vazio)/`dead`. (3) AUDIT de requeue — `reenfileirar_dlq` (`router_admin.py`) trocou `dependencies=[Depends(exigir_papel("admin"))]` por param nomeado `usuario: dict = Depends(exigir_papel("admin"))` (FastAPI aplica o guard identico nas duas formas; a nomeada ainda injeta o payload) e loga `outbox_reenfileirado_via_admin` info com `outbox_id`+`ator` (=`sub` do JWT, fallback `email`, via helper puro `_ator_de`). Audit emitido APOS confirmar a mutacao (nao no 404) — reflete so o que de fato aconteceu (teste afirma ausencia de audit no 404). (4) GATE de seguranca — bandit `targets` (pyproject) + `make {lint,format,typecheck,security}` agora incluem `relay`+`scripts` (antes so `src`,`ui`). Findings reais corrigidos: `scripts/outbox_dlq.py` TC002 (`Engine` -> TYPE_CHECKING), `__main__.py`/`rewrite-md-links.py` I001, `processador.py` E741 (`l`->`row`/`linha`); 2 deliberados com `# nosec`+`# noqa` e razao (B108 `/tmp/relay-heartbeat` efemero; B110 cleanup best-effort do autocommit no shutdown) — GOTCHA: prosa apos `# nosec BXXX` faz bandit emitir "Test in comment: X is not a test name" (parseia as palavras como test-ids); por a razao num `#` comment SEPARADO/acima e deixar `# nosec BXXX` sozinho. PLR0913 dos seams injetaveis (`_drenar`/`processar_ciclo`, 6 args) via per-file-ignore `relay/**`. Os 2 asserts de narrowing de `driver_connection` viraram guarda `if None: raise` (`-O`-safe) e o 2o foi eliminado reusando a var ja narrowed. (5) COMENTARIO da invariante de lease/HA em `listener.py` (`_LEASE_PADRAO`) e `processador.py` (`processar_ciclo` docstring): lease DEVE exceder a latencia de pior caso de 1 handler (limitada pelo timeout do SMTP); `replicas:1` + drain sequencial = seguro; `replicas>1` exige a invariante (ou fencing na entrega) senao lease-expiry mid-delivery duplica e-mail. `replicas:1` shipado NAO mudou. GOTCHA do gate: `make security` roda `--severity-level high` (so High bloqueia); relay/scripts ficam 0 High/0 Medium (Low aceitos, consistente com a postura src/ui). Testes: unit de resiliencia (`test_listener.py`, fakes de engine/raw + `select` monkeypatchado, `processar_ciclo` que raise no 2o call=1o drain in-loop) + integ do gauge (`test_relay_fluxo.py`, seeds pendente/dead + capture_logs) + integ do audit (`test_admin_outbox.py`, fixture autouse trocando `router_admin._log` pelo gotcha do cache_logger) + unit puro de `_ator_de` (`test_router_admin.py`, ramos email/None). `make test` 1416 verde (cov 96.88%), `make test-integ` 135 verde, lint/typecheck/security limpos (relay+scripts agora escaneados), lint-arch 3 kept 0 broken
- 2026-06-25 - [feat/td-008-transactional-outbox] BUG do outbox TD-008 corrigido: o relay marcava e-mail `entregue` mesmo com SMTP fora, tornando retry/backoff/DLQ inalcancaveis. Causa: `NotificarMudancaDeStatus.__call__` (`src/ordem_servico/aplicacao/notificacoes.py`) engolia TODA excecao do envio (`except Exception: _log.exception(...)`) — contrato da fase sincrona antiga (falha de envio nao podia bloquear a transicao). Mas o relay e o UNICO caller agora (dispatcher sincrono esvaziado: `interfaces/dependencies.py` -> `EventDispatcher(handlers=())`), e `processar_linha` (`relay/processador.py`) trata "handler retornou normal" como sucesso -> `registrar_processed` + `marcar_entregue`. Fix CIRURGICO: o `except` agora pega so transporte (`except (OSError, smtplib.SMTPException)`), loga e RE-RAISE; os early-returns de skip (ordem/cliente/e-mail ausentes) seguem nao-fatais (retentar nao resolve "nada a enviar" -> `entregue` correto). `ValueError` de `SMTP_PORT` mal formado nao e pego aqui -> propaga ao relay (vira DLQ visivel, melhor que `entregue` silencioso). Adapter `SmtpEmailAdapter.enviar` JA propagava (docstring "Falhas... PROPAGAM"); o swallow era so no handler. Relay NAO mudou (seu `except Exception` ja dirige retry/dead). GAP que escondeu o bug: todos os testes de relay injetavam handlers FAKE que `raise` — o caminho REAL (`construir_mapa_handlers` -> handler real + adapter real) nunca era exercitado contra SMTP morto. Fechado por `tests/integracao/relay/test_relay_smtp_falha.py::test_falha_de_smtp_no_handler_real_vai_para_dlq` (testcontainers): cliente/veiculo/OS REAIS commitados + outbox real + `SMTP_HOST=127.0.0.1 SMTP_PORT=9` (porta fechada -> `ConnectionRefusedError` IMEDIATO, sem o timeout de 5s×5 do adapter), 5 ciclos antecipando `proxima_tentativa_em` -> linha vira `dead` (nao `entregue`), 0 `processed_events`. Provado que pega o bug: revertendo o fix o teste falha com "obtido 'entregue' — handler engoliu a falha". GOTCHA do teste: o relay abre PROPRIA session (`construir_mapa_handlers`) contra o `engine`, entao o seed PRECISA commitar de verdade (`sess.commit()`, nao o savepoint da fixture `session`) — e por isso e o unico teste de integracao que commita; cleanup ESCOPADO por id (ordem->veiculo->cliente) em `finally`, nunca `DELETE` global das tabelas (acoplaria com os outros). `make test` 1404 verde (cov 96.99%), `make test-integ` 122 verde, lint/mypy limpos, lint-arch 2 kept 0 broken
- 2026-06-20 - [chore/migracao-refs-org] Repo TRANSFERIDO de `jbamaral/postech-sw-arch-p2` para a org `fiap-postech-sw-architecture/postech-sw-arch-p2` (transfer do GitHub: PRs/historico/Actions preservados — 23 PRs mergeados, HEAD a20e4be intacto, runs antigos 27450493913/27451618014 resolvem na URL nova; URL antiga redireciona). Acao: reescrita global `jbamaral/postech-sw-arch-p2` -> `fiap-postech-sw-architecture/...` em README, docs/entrega/fase2/{entrega-fase-2,roteiro-video}.md, k8s/{README,deployment.yaml}, scripts/build-entrega-pdf.sh (`--repo` do rewrite de links do PDF). NAO alterados (corretos por desenho): (a) `.github/workflows/cd.yml` usa `${GITHUB_REPOSITORY,,}` -> auto-resolve pro novo owner; (b) Makefile `GHCR_USER=jbamaral`/`GHCR_PREFIX=postech-sw-arch-p1` + `db-image/*` sao o fast-check da FASE 1, cujas imagens vivem mesmo sob `jbamaral` no GHCR. SUPERSEDE o aprendizado (a) da entrada [in-flight from docs/i12-entrega-fase-2] (2026-06-12) "a org NAO tem p2": a org AGORA tem p2; remote local `origin` atualizado pra SSH do org. PENDENTE: `soat-architecture` confirmado NAO-colaborador e NAO-membro do org (HTTP 404) — convite do avaliador segue como pendencia #1 da entrega, agora no nivel do repo/org `fiap-postech-sw-architecture`.
- 2026-06-12 - [in-flight from docs/i13-hardening-final] Fechamento da fase 2 (I13, hardening): implementacao em 11 PRs (#12-#22, 2026-06-12) + entrega em PR #23. Cobertura final confirmada local: 97.52% src (1318 unit) + 98.48% ui (gate >=95 nos dois); 103 testes de integracao verdes. Scans de fechamento em `docs/seguranca/scan-fase-2.md`: bandit 1.9.4 0 high/0 medium (1 low aceito: B105 fallback dev-only da ui); pip-audit 2.10.1 achou 11 advisories em 5 pacotes e a ACAO foi upgrade no uv.lock (pyjwt 2.13.0, starlette 1.0.1 — fixado na versao exata do fix em vez da latest 1.3.1 pra minimizar drift —, urllib3 2.7.0, idna 3.18, mako 1.3.12; re-scan limpo, suite verde); gitleaks 8.30.1 0 leaks wt+historico (164 commits) apos 4 paths + 1 regex novos no `.gitleaks.toml` (db-image compose = Fernet demo fase 1; trivy report = hashes; infra tfstate/kubeconfig = gitignored locais; token demo do webhook RF-022). ADRs 015-021 promovidos a Aceita (padrao fase 1: so flip da linha Status, sem "Aceita em") e ADR-003 marcado `Parcialmente substituida pela ADR-015` no proprio campo Status (estava Proposta no arquivo apesar de Aceita no README — divergencia historica resolvida pela supersessao). Tech-debt ganhou secao Fase 2 (TD-015..TD-020: corrida de migracao multi-replica, limiter por-pod, PII na query string dos traces, db-image -p1, aplicacao->infra na autenticacao, kanban sem encerradas). Gap-analysis §1 marcado IMPLEMENTADO por PR (RNF-024 marcado PARCIAL — pool segue nos defaults) e §4 com veredito final aplicado. GOTCHA: pip-audit roda contra o ambiente — `uvx pip-audit` audita o venv do proprio uvx (28 pacotes, resultado inutil); o comando certo e `uv run --with pip-audit pip-audit`. Testcontainers local exige `DOCKER_HOST=unix://$HOME/.colima/default/docker.sock` + `TESTCONTAINERS_RYUK_DISABLED=true` (docs/setup/troubleshooting.md)
- 2026-06-12 - [in-flight from docs/i12-entrega-fase-2] Pacote de entrega da fase 2 em `docs/entrega/fase2/` (entrega-fase-2.md espelha o formato da fase 1 com tabela de rastreabilidade requisito->PR->evidencia->bloco do video; roteiro-video.md cronometrado ~14min30s; postman_collection.json gerada do openapi.json VIVO com `npx -y openapi-to-postmanv2 -s ... -p -O folderStrategy=Tags` — 46 requests, RF-020..024 conferidos na collection) + README raiz reescrito pra fase 2 (mermaid copiado byte-identico da RFC-002 §3 com comentario `<!-- fonte: RFC-002 §3 -->`; placeholder de video `<!-- VIDEO-LINK-FASE-2 -->` no README e no entrega-fase-2.md). Aprendizados: (a) o repo canonico da fase 2 e `jbamaral/postech-sw-arch-p2` (a org fiap-postech-sw-architecture NAO tem p2) e `soat-architecture` AINDA NAO e colaborador (convite pendente so de allan66) — pendencia #1 da secao 8 do entrega-fase-2.md; (b) `db-image/` ainda puxa imagens `-p1` do GHCR (sem RF-020..024/mailpit) — fast-check rebaixado no README a atalho da fase 1 com aviso explicito; atualizar ou aposentar pos-banca; (c) PDF de submissao scriptado em `scripts/build-entrega-pdf.sh` (rewrite-md-links.py + mermaid-cli rendendo o diagrama em PNG + pandoc --pdf-engine=weasyprint; artefato FORA do repo em `~/git/fiap/postech-sw-architecture/documento-entrega-fase-2.pdf`); pandoc nao renderiza mermaid — sem o passo do mmdc o diagrama sai como bloco de codigo no PDF; (d) cobertura real da main confirmada na CI (run 27451618008): 97.52% src + 98.48% ui; (e) ADRs 015-021 seguem `Status: Proposta` — promover a Aceita (e marcar ADR-003 como parcialmente substituido, como o proprio ADR-015 preve) e follow-up pos-entrega
- 2026-06-12 - [PR #22] ADR-020 implementado em escopo minimo: `configurar_otel(app, engine)` em `src/compartilhado/infraestrutura/observability.py`, chamado no lifespan (unico ponto com app+engine juntos), default OFF via `OTEL_ENABLED` (desligado = zero import de otel). Decisoes: (a) extra `otel` FORA do extra test — CI nao paga SDK+grpcio; imports lazy com try/ImportError (flag on sem extra = warning acionavel e boot segue) + override mypy `opentelemetry.*` ignore_missing_imports (mypy do CI nao ve os pacotes; rodado verde TAMBEM com o extra instalado); (b) par pinado SDK 1.42.x <-> instrumentation 0.63bx (sdk exige semantic-conventions ==0.63bx; atualizar os 4 juntos em milestone); (c) exporter OTLP/gRPC 4317, `insecure` derivado do scheme `http://`; tracer_provider explicito nos 2 instrumentadores em vez de set_tracer_provider global (warm restart logaria "Overriding ... not allowed"); (d) GOTCHA CENTRAL: instrumentation-fastapi 0.63 nao adiciona middleware — embrulha `build_middleware_stack`, e o proprio scope lifespan ja construiu o stack; instrumentar no lifespan exige `app.middleware_stack = app.build_middleware_stack()` no final, senao spans de sqlalchemy aparecem e os de fastapi NAO (sintoma validado contra Jaeger real antes/depois do fix); (e) Dockerfile instala `--extra otel` nas duas syncs — imagem unica ligavel so por env; custo medido +37MB (462MB->499MB em A/B no mesmo baseline: grpc 17MB + opentelemetry 6MB + protobuf 4MB); (f) jaeger all-in-one:1 e workload de demo no padrao mailpit (k8s/jaeger.yaml entra no `kubectl apply -f k8s/` existente do CD; no compose sob `profiles: ["otel"]`, fluxo normal nao sobe). Evidencia real: `/api/services` lista pytstop-api; trace do login com 5 spans fastapi + connect/SELECT sqlalchemy; `/api/v1/saude` excluida (0 spans com healthcheck batendo)
- 2026-06-12 - [PR #21] RNF-022/ADR-019 implementado: `.github/workflows/cd.yml` (push main + workflow_dispatch; job `image` builda e publica `ghcr.io/<repo>-app:<sha>` com GITHUB_TOKEN, job `deploy` sobe kind efemero no runner via terraform de infra/, `kind load`, metrics-server, manifests k8s/, `set image` pra tag do SHA, rollout e smoke) + alvos make `k8s-up`/`k8s-smoke`/`k8s-down`/`cd-local` espelhando o workflow. Gotchas reais: (a) o job deploy PRECISA de `docker login` proprio antes do `docker pull` — GHCR privado nega pull sem auth mesmo com a imagem recem-publicada pelo job anterior (o template original omitia); (b) fluxo local usa tag por SHA do HEAD + `set image` (nao a tag `dev` do manifest): o RS transitorio com `:dev` fica em ErrImagePull e e superado pelo set image — `rollout status` reporta "1 old replicas pending termination" e completa (mesmo comportamento do CD); alteracoes nao commitadas reusam a tag e viram no-op (documentado no Makefile); (c) patch `--kubelet-insecure-tls` do metrics-server guardado por grep nos args — re-aplicar o `--type json add` sem guard duplicaria o arg a cada k8s-up; (d) smoke em 127.0.0.1:18000 (nao 8000) pra nao colidir com a stack compose — colisao faria o smoke passar contra o container compose em vez do cluster; (e) pilha completa (kind + postgres + api + mailpit + metrics-server) NAO coube confortavel em colima 2GiB — subido pra 4GiB (`colima stop && colima start --memory 4`); evidencias: api/mailpit 1/1 Running, HPA `cpu: 4%/70%, memory: 30%/80%` ~40s pos-deploy, `kubectl top` api 4m/77Mi; (f) kubectl dos alvos make usa `--context kind-$(K8S_CLUSTER)` explicito (nao muda current-context do dev); o workflow usa `kubectl config use-context` (runner descartavel); `K8S_CLUSTER` alimenta o `-var cluster_name` do terraform — um knob so pra cluster paralelo; (g) rtk bufferiza stdout de comando longo em background (arquivo de output fica 0B ate concluir) — monitorar `make cd-local` exige foreground via `rtk proxy`
- 2026-06-12 - [PR #20] RNF-021 implementado em `/infra` (Terraform: kind_cluster + namespace pytstop-infra + Secret + StatefulSet postgres:16 + Service ClusterIP num unico apply; lock file COMMITADO pinando tehcyx/kind 0.11.0 + kubernetes 2.38.0). Learnings operacionais: (a) o provider tehcyx/kind grava kubeconfig em `infra/<cluster>-config` (gitignorado — credencial dentro do repo) E mescla contexto `kind-<nome>` no `~/.kube/config`; (b) `pg_isready -h 127.0.0.1` evita falso-Ready durante o servidor temporario do initdb (que escuta so socket unix) e `PGDATA` em subdiretorio evita o initdb recusar mount nao-vazio; (c) `wait_for_rollout` do kubernetes_stateful_set faz o apply so terminar com postgres Ready — apply verde == banco Running (evidencia RNF-021 pro video); (d) kind+postgres rodam em colima 2CPU/2GiB; ferramentas via brew: hashicorp/tap/terraform (formula core congelada <1.7), kind, kubernetes-cli; (e) apply real validado: 5 recursos em ~60s, psql autenticado, destroy limpo (k8s antes do cluster); clusters de branches irmas coexistem via `var.cluster_name`
- 2026-06-12 - [PR #19] RNF-020/RNF-023 implementados em `/k8s` (Namespace, ConfigMap, Secret, Deployment, Service, HPA autoscaling/v2 cpu 70/mem 80 1-5 replicas, Mailpit), probes na rota real `/api/v1/saude`. Decisoes: (a) `DATABASE_URL` usa DNS cross-namespace `postgres.pytstop-infra.svc.cluster.local` — banco vive no ns `pytstop-infra` do Terraform I9 (ADR-017), app no ns `pytstop`; (b) secret.yaml commita VALORES DE DEMO marcados linha a linha ("Demo only"; producao = SealedSecrets/External Secrets, RFC-002 §6); chave Fernet de demo gerada NOVA (nao reusa a do .env.dev.example) e anotada `# gitleaks:allow` inline — `k8s/` nao esta no allowlist de paths do `.gitleaks.toml` e o `generic-api-key` dispara em base64 de 44 chars; (c) `replicas: 1` explicito e deliberado (migracao no boot exige rollout inicial de replica unica, ADR-019); reaplicar o Deployment com HPA ativo reduz momentaneamente para 1 — aceito no cluster de demo e comentado no YAML; (d) `RUN_SEED_ON_STARTUP=true` no cluster (admin demo via Secret, best-effort) e `ENVIRONMENT=development` (production desabilitaria /docs e a banca valida pelo Swagger); (e) `kubectl apply -f k8s/` processa o diretorio em ordem ALFABETICA e namespace.yaml nao vem primeiro — num cluster novo aplicar `k8s/namespace.yaml` antes do diretorio (README documenta); (f) gotcha de validacao: `kubectl apply --dry-run=client` no kubectl 1.36 baixa o openapi do cluster e FALHA sem cluster acessivel — validado com dry-run client+server e apply real em cluster kind descartavel (mailpit chegou a Running; api em ErrImagePull esperado sem `kind load`)
- 2026-06-12 - [PR #18] RNF-019 implementado com healthcheck do app nos DOIS lugares (HEALTHCHECK no Dockerfile + bloco `healthcheck` no servico `app` do compose, comandos identicos): probe `python -c` com urllib em `http://127.0.0.1:8000/api/v1/saude` (imagem slim nao tem curl/wget — mesmo padrao do db-image/PR #139), interval 30s / timeout 3s / retries 3 / start_period 20s. Compose com `healthcheck:` proprio tem precedencia total sobre o da imagem; manter os dois e deliberado (RNF-019 pede ambos; o do compose vale mesmo contra imagem stale de registry). `ui.depends_on.app` PROMOVIDO de service_started para service_healthy com base em medicao, nao em suposicao: a estimativa inicial era +30s por boot (primeiro probe so no interval), mas Docker Engine 25+ proba a cada `start_interval` (default 5s) DURANTE o start_period — app healthy em ~3-5s observado, stack fria completa (build cacheado) `up -d` retorna em 14s com sequencia app Healthy → ui Started. Em engine pre-25 o primeiro probe cai no interval de 30s (documentado no YAML). Fluxos dependentes conferidos: `make reset-db` (poll curl do host roda DEPOIS do up, passa na 1a tentativa) e full-test CI (`wait_for_saude` 60s tambem pos-up) nao mudam; falha de app agora aborta o `up` com "dependency failed to start" em vez de ui no ar contra backend morto
- 2026-06-12 - [PR #17] RF-024 implementado ligando o dispatch deferido desde a fase 1: `EventDispatcher` minimo em `src/ordem_servico/aplicacao/dispatcher.py` (sincrono in-process, lista de handlers, engole+loga falha de handler) injetado como kwarg opcional (`dispatcher=None`) nos 9 use cases de transicao — `_despachar_pos_commit` roda DEPOIS do `with uow` (handler ve a OS persistida; provado em unit com FakeUoW e em integracao lendo a linha no banco dentro do handler) e NAO limpa `_eventos_pendentes` (testes existentes inspecionam eventos pos-executar; agregados sao request-scoped, sem risco de redispatch). `CriarOrdem` nao despacha por desenho (criacao nao e atualizacao de status); `DecidirOrcamento` herda o dispatch dos use cases que compoe. Decisoes nao-obvias: (a) destinatario via extensao do `ClientePort` (`obter_contato` -> `ClienteContatoDTO` id/nome/contato; adapter le `clientes_table` direto — `contato` e plaintext, so `documento` e cifrado); (b) validacao de e-mail por regex simples com `search` em `aplicacao/notificacoes.py` — contato e campo LIVRE ("Maria - x@y.com / tel"), entao EXTRAI o primeiro token com forma de e-mail; EmailStr/pydantic validaria o campo inteiro (e acoplaria framework na aplicacao); sem e-mail -> skip + warning; (c) rotulo de situacao MOVIDO de `interfaces/presenters.py` para `src/ordem_servico/aplicacao/situacoes.py` (aplicacao nao importa interfaces; lint-imports KEPT) com presenters reexportando via alias `as` — imports de schemas/testes preservados, guard de identidade em `test_presenters.py`; (d) `SmtpEmailAdapter` le env DENTRO de `enviar()` (nao no `__init__`): o adapter e construido no composition root antes da transicao rodar e um `SMTP_PORT` mal formado no construtor derrubaria a request inteira; lendo no envio vira falha engolida pelo handler; (e) e-mail handler SEMPRE ligado (sem flag de ambiente) — em test/dev sem SMTP a conexao recusada e logada e engolida; e2e troca o adapter monkeypatchando `dependencies.obter_email_port` (factories de OS nao sao `Depends`; override e no composition root); (f) compose: servico `mailpit` (container pytstop-mailpit, so a UI 8025 publicada; SMTP 1025 interno) + defaults `SMTP_*` no `environment` do app (mesmo racional de precedencia do ORCAMENTO_WEBHOOK_TOKEN)
- 2026-06-12 - [PR #16] RF-022/ADR-021 implementado por composicao: `DecidirOrcamento` (`use_cases.py`) compoe `AprovarOrcamento`/`AprovarOrcamentoComplementar`/`CancelarOrdem` (zero regra duplicada) e a rota `POST /api/v1/publico/ordens-de-servico/{id}/decisao-orcamento` vive no router publico de compartilhado (mesmo trilho lazy-import do acompanhamento), nao num router proprio de OS. Decisoes nao-obvias: (a) recusa externa SEMPRE cancela — inclusive em AGUARDANDO_APROVACAO_COMPLEMENTAR (desistencia total, com liberacao de reserva via CancelarOrdem); a rejeicao parcial do complementar (volta a EM_EXECUCAO) continua exclusiva do endpoint interno admin `rejeicao-complementar`; (b) guard de estados de espera dentro do use case e OBRIGATORIO: sem ele, recusa externa em OS RECEBIDA/EM_EXECUCAO viraria cancelamento generico (CancelarOrdem aceita qualquer estado ativo) — coberto por teste negativo unit+e2e; (c) `ORCAMENTO_WEBHOOK_TOKEN` lido por request (`os.environ.get`, padrao JWT_SECRET) e comparado com `secrets.compare_digest` sobre bytes; nao configurado → 503 "canal desabilitado" (nunca canal aberto sem credencial), header ausente/errado → 401 via HTTPException detail (padrao middleware, nao envelope de dominio); (d) resposta reusa `AcompanhamentoResponse` — canal externo nao vaza itens/orcamento/cliente (LGPD), e2e afirma a ausencia; (e) docker-compose: `environment` vence `env_file` — o default demo no compose cobre `.env.dev` copiado antes do RF-022, comentario no YAML avisa a precedencia e a sincronia com `.env.dev`
- 2026-06-12 - [PR #15] RF-020 implementado com `CriarOrdemDTO` ganhando `servicos`/`pecas` (tuplas, default vazio — fase 1 intacta) e a montagem de linha extraida para `_montar_item` em `use_cases.py`, fonte unica compartilhada com `AdicionarItem` (validacao servico existe/ativo, peca existe, preco da fonte certa). Decisoes nao-obvias: (a) entrada de `pecas[]` EXIGE `servico_catalogo_id` — invariante de `ItemDaOrdem` + coluna NOT NULL (`mapping.py:83`); toda peca e consumida por um servico, mudar isso pediria migration fora do escopo; (b) saldo de estoque NAO e validado na criacao — reserva/validacao continua exclusiva da aprovacao (ADR-008); criar OS com qty > saldo da 201 e o erro `EstoqueInsuficienteException` (409) aparece na aprovacao com rollback all-or-nothing (provado em `test_api_e2e.py::TestCriacaoOsComItens` com 2 pecas: a de saldo ok nao fica reservada); checagem advisory na criacao seria TOCTOU sem lock e rejeitaria reposicao de estoque entre abertura e aprovacao; (c) `AdicionarItemDTO.descricao` virou `str | None` — `None` = resolver do nome do servico/peca via port (payload RF-020 nao carrega descricao livre); `""` explicito segue violando a invariante do dominio (`is None`, nao falsy); o schema HTTP de adicionar item continua exigindo descricao. Atomicidade: itens montados em memoria antes do unico `repo.salvar` + commit dentro da UoW
- 2026-06-12 - [PR #14] RF-021 implementado como apresentacao pura: `situacao_de` em `src/ordem_servico/interfaces/presenters.py` (dict StatusOrdem -> rotulo; valores persistidos intocados, gap §2) e `situacao` como `computed_field` derivado de `status` em `OrdemDeServicoResponse`/`OrdemResumoResponse`/`AcompanhamentoResponse` (os tres canais do RF-021, incluindo a consulta publica). Computed field em vez de preenchimento no router foi deliberado: 13 endpoints devolvem o detalhe via `model_validate(from_attributes)` de DTO sem o campo — derivar no schema elimina drift por call-site esquecido e poe o rotulo no OpenAPI (descricao lista os 7 rotulos; criterio de aceite RF-021). Precisa do `# type: ignore[prop-decorator]` (mypy strict nao suporta decorator sobre @property; padrao documentado do pydantic). Grafia dos rotulos: gap §2 + plano I3 adotam "Em diagnóstico"/"Em execução" (sentence case, acento — user-facing string, ADR-009 ok), apesar de a lista literal do challenge (desafio-tech-fase-2.md:35) omitir o "Em" em dois itens; a propria ordenacao do challenge (linha 39) usa "Em Execução". AGUARDANDO_APROVACAO_COMPLEMENTAR apresenta o rotulo de AGUARDANDO_APROVACAO (gap §2/RN-020); CANCELADA -> "Cancelada". Guard de totalidade em `test_presenters.py` espelha o do I2
- 2026-06-12 - [PR #13] RF-023 implementado como read-side puro: `case()` de prioridade + filtro `notin_` no repository SQLAlchemy (`_PRIORIDADE_STATUS`/`_ESTADOS_ENCERRADOS` em `src/ordem_servico/infraestrutura/repository.py`), parametro `incluir_encerradas` atravessando Protocol -> use case -> router. Decisoes: (a) `contar()` ganhou o mesmo filtro com default `True` (assimetrico de proposito — preserva semantica "total persistido" de `ObterMetricas` sem toca-la; `ListarOrdens` repassa explicito) para o `total` da paginacao acompanhar o universo filtrado; (b) Protocol `dominio/repository.py` mudou de assinatura (porta de leitura, nao regra de negocio — maquina de status intocada); (c) teste-guarda em `test_repository_os.py` exige que prioridades + encerrados cubram todos os `StatusOrdem` (novo status sem classificacao quebra o teste em vez de degradar em silencio); (d) cenarios de listagem em teste inserem linhas direto via `ordens_de_servico_table.insert()` para fixar `status`/`criado_em` sem percorrer transicoes. Pendencias conhecidas fora do escopo I2: `ui/` continua chamando a listagem sem `incluir_encerradas` (kanban deixa de mostrar encerradas — adaptar junto com RF-020 conforme gap §4) e nao ha indice para o sort por CASE+criado_em (escala atual nao justifica)
- 2026-06-10 - Fase 2 iniciada. Repo semeado com o historico completo do p1 via branch `seed/historico-fase-1` (merge de historias nao relacionadas, sem squash; force-push bloqueado por branch protection + API GitHub inacessivel na rede). Decisoes formais antes de qualquer codigo: ADR-015 (Clean Architecture sem rewrite, nucleo ports/adapters preservado), ADR-016 (kind unico dev/video/CI, cluster via Terraform `tehcyx/kind`, CI em cluster efemero no runner), ADR-017 (PostgreSQL 16 StatefulSet+PVC via Terraform em `/infra`; app em `/k8s`), ADR-018 (EmailPort em `aplicacao/` + adapter SMTP + Mailpit), ADR-019 (CD em push na main: GHCR tag por SHA, `kind load`, migracoes via entrypoint com replica unica inicial), ADR-020 (OpenTelemetry minimo como onda final condicional) e RFC-002 (design integrado + diagrama mermaid). Requisitos novos na gap analysis: RF-020..024, RNF-017..024, RN-018..020 (`docs/requisitos/fase2/gap-analysis-fase-2.md`). Processo da fase 2: superpowers (spec → plan → execucao) com artefatos em `local/postech-bootstrap/docs/superpowers/` (fora deste repo); review canonico single-shot + Judge (perspectivas arquivadas)
- 2026-05-04 - [in-flight from PR #146] Convencao de breadcrumb adotada em todos os 63 docs aninhados: `> [↑ Raiz do projeto](rel/path/README.md) · [↑ Pai](rel/path/parent.md)` no topo (linha 3 apos H1) e como ultima linha nao-vazia. README.md/CONTRIBUTING.md raiz nao recebem (sao destino raiz). ADRs sob `docs/arquitetura/adr/` apontam pai pra `docs/arquitetura/README.md` (nao existe `adr/README.md`). Verificacao via `scripts/lint-doc-anchors.sh` (executavel) — exclui apenas `README.md`/`./README.md`/`CONTRIBUTING.md`/`./CONTRIBUTING.md` literal-bare-filename pra nao pular READMEs aninhados. PDF da entrega (autocontido) gerado com `scripts/rewrite-md-links.py` + `pandoc --pdf-engine=weasyprint` (xelatex nao instalavel sem sudo); artefato fica em `~/git/fiap/postech-sw-architecture/documento-entrega-fase-1.pdf` (fora do repo). Issue #145 documenta decisoes por categoria + tabela de pendencias da entrega
- 2026-05-04 - Padrao de revisao em massa de docs: 7 sub-agentes especialistas em paralelo por arquivo (detector tracos IA, limpador comentarios, redutor tamanho, leitor humano, leitor engenheiro, link checker, navegabilidade) + 8o (banca examinadora FIAP) so pra documento mestre da entrega. Synthesis APPLY/REJECT/DEFER por finding, justificativa registrada. Um commit por arquivo no branch; squash-merge na PR. Util pra prep de entregas onde precisa de auditabilidade granular sem inflar contexto principal (subagentes isolam por perspectiva)
- 2026-05-03 - [in-flight from PR #139] `db-image/QUICKSTART.md` virou single source of truth do fast-check; banner do README raiz ficou um link curto pra ele (eliminada duplicacao das instrucoes de PAT/login/up). Pagina de login da UI agora exibe SHA do commit no rodape (`vXXXXXXXXXXXX`), lida de `PYTSTOP_GIT_SHA` (env injetada via build arg do `ui/Dockerfile`) -- 3 lugares pra validar versao da stack rodando: logs de boot, rodape da UI, LABEL OCI `org.opencontainers.image.revision`. `Config.git_sha` tem default `""` pra cobrir rodada local sem build (uvicorn direto pra hot-reload), onde a env var nao existe; o `if CONFIG.git_sha` no login.py simplesmente nao renderiza o rodape nesse caso. Cross-referenciado na secao 5 do QUICKSTART
- 2026-05-03 - `ClienteRepository.bloquear_veiculo_para_remocao` virou `(veiculo_id) -> bool` em vez de `-> None`. True = linha travada com `FOR UPDATE`; False = linha ja sumiu (caso de duplo-DELETE concorrente). `RemoverVeiculo.executar` levanta `VeiculoNaoEncontradoException` (404) quando o lock retorna False, em vez de prosseguir e deixar o `flush` emitir DELETE de zero linhas → `StaleDataError` → 500. Issue #128
- 2026-05-03 - Testes da UI que usam o fixture `screen` do nicegui.testing devem usar o marker `@pytest.mark.module_under_test(modulo)` em vez de manipular `sys.modules` + `reload` na mao. O fixture `nicegui_reset_globals` zera `Client.page_routes` entre testes; `prepare_simulation` reaplica os decoradores `@ui.page` recarregando o modulo dentro do auto-index client. O marker e declarado em `pyproject.toml` `[tool.pytest.ini_options].markers` para sobreviver a coleta sem o extra `ui` (--strict-markers). Issue #130
- 2026-05-03 - `webdriver-manager` removido do extra `ui` em `pyproject.toml`/`uv.lock`: zero callsite no repo, e Selenium >= 4.6 ja inclui `selenium-manager` builtin que resolve o ChromeDriver automaticamente. Issue #131
- 2026-05-02 - Mitigacoes Sonar em seguranca: `src/main.py` nao contem mais `DATABASE_URL` default com senha; em `development`/`test`, ausencia de `DATABASE_URL` exige `POSTGRES_DB`, `POSTGRES_USER` e `POSTGRES_PASSWORD` para montar a URL em runtime. `python src/main.py` agora binda `127.0.0.1` por padrao via `UVICORN_HOST`; Docker continua usando `entrypoint.sh --host 0.0.0.0`, onde bind em todas as interfaces e necessario para expor a porta do container. `JWTService.validar_token` tambem deixou de ler header JWT nao verificado e delega algoritmo/assinatura ao `jwt.decode(..., algorithms=["HS256"])`
- 2026-05-01 - [in-flight from PR fix/all] `RemoverVeiculo` agora bloqueia em qualquer OS vinculada (ativa ou encerrada), nao so em OS ativa. Razao: `ordens_de_servico.veiculo_id` tem FK sem ON DELETE CASCADE; deletar veiculo com OS historica caia em IntegrityError → 500. A porta `OrdemDeServicoPort` foi renomeada `existe_os_ativa_para_veiculo` → `existe_os_para_veiculo` e a query interna nao filtra mais por status. Mudou tambem o contrato `ClientePort.veiculo_existe(veiculo_id)` → `veiculo_pertence_ao_cliente(cliente_id, veiculo_id)`, fechando a brecha de criar OS associando veiculo de outro cliente. A semantica de propriedade vira invariante validada antes de criar a OS
- 2026-04-29 - Fix #83 (PR #117 ja em main): ValueError em invariantes de dominio agora retorna **HTTP 422 VALOR_INVALIDO** via handler global em `error_handler.py`, em vez de cair no fallback 500 ERRO_INTERNO. Escolha consciente da abordagem handler-only (Solucao 5 do triage da issue) em vez de migrar 57 `raise ValueError` em 20 arquivos de `src/` para `ValorInvalidoException(DomainException)` -- mantemos intactos os 94 sites de teste com `pytest.raises(ValueError, ...)`. Trade-off: ValueError acidental (lib externa, bug interno) tambem vira 422 em vez de 500; ate aqui as mensagens de domain raise sao todas rotulos curtos sem PII (auditado via grep). Migrar para subclasse fica como tech-debt se o trade-off vazar
- 2026-04-29 - Read-side cross-context enrichment (resolver nomes a partir de IDs de outros bounded contexts) vive em `<contexto>/aplicacao/queries.py` via query handler que orquestra Ports em batch — NUNCA no router. CatalogoPort/EstoquePort ganharam `obter_servicos_em_lote(ids: set[UUID])` e `obter_itens_em_lote(ids: set[UUID])` que retornam `dict[UUID, DTO]`; adapters concretos short-circuitam em `set` vazio sem tocar a session. ItemDaOrdemDTO carrega `servico_nome`/`item_estoque_nome` opcionais (default None) preenchidos por `EnriquecerOrdemDeServico.executar(ordem_dto)` antes do mapping pra Pydantic. Pattern aplicavel a qualquer endpoint que precise hidratar IDs cross-context — issue #87
- 2026-04-29 - LGPD anonymization: VO `DocumentoAnonimizado(cliente_id: UUID)` substitui o patch tatico `numero="ANONIMIZADO"` em `CPF.__new__` (PR #78). Mapping reidrata como VO de primeira classe; `isinstance(doc, CPF/CNPJ)` volta a ser coerente para anonimizados. Tipo "anonimizado" adicionado a `_tipo_documento` helper em `aplicacao/use_cases.py`. Issue #79
- 2026-04-29 - ADR-014 (gerenciador de pacotes `uv`) promovida de Proposta para Aceita; uso na pratica ja consolidado em `Makefile`, `Dockerfile`, guias de setup e `docs/desenvolvimento.md`. Fallback `python -m venv` + `pip install` mantido apenas como contingencia -- issue #84
- 2026-04-29 - README raiz reorganizado: ponto de entrada com Quick Start canonico (`make reset-db`) + links curtos para detalhes; dev workflow movido para `docs/desenvolvimento.md`; troubleshooting cross-platform de Docker em `docs/setup/troubleshooting.md`. `ui/README.md` aponta para README raiz como "como rodar" e cobre apenas o que e especifico da UI -- elimina Quick Start duplicado entre raiz e ui/ -- issue #84
- 2026-04-28 - Correção de inventário no apêndice de funcionalidades extras: o arquivo lista 19 features (5+3+2+3+3+3), não 14 como a entrada de 2026-04-27 indicou; entrada anterior preservada por add-only mas o número canônico no documento é 19
- 2026-04-27 - Apêndice de funcionalidades extras criado em `docs/entrega/apendice-funcionalidades-extras.md`; documenta 14 features além do escopo mínimo do desafio FIAP, ancoradas em commits/PRs (#19, #62, #64, #65, #75, #81)
- 2026-04-27 - 10 issues abertas (#99–#108) no `fiap-postech-sw-architecture/postech-sw-arch-p1` para fechar gaps de entrega (soat-architecture access, Discord, vídeo, PDF, 6 scans de segurança); cada issue formatada como playbook executável por agente (workflow A/B/C/D)
- 2026-04-26 - Added the NiceGUI-based UI of simulation (`ui/`) as a sandbox for manual API testing -- not a production deliverable, dev-only, not packaged in `pyproject.toml` `setuptools.packages.find` -- PR #81
- 2026-04-26 - Adopted unified harness pointers around `postech-ai-helper/ai/agent-bootstrap.md` -- prior per-harness configs duplicated content and drifted -- PR on `postech-ai-helper/feat/agent-bootstrap-and-memory`

## Discovered conventions

- 2026-07-02 - **Teste de mesa inclui a suite mista com os testes lentos** (pedido do usuario): `uv run --no-sync pytest tests/unitarios -q` SEM `-m "not lento"` (flag `--no-lint` removida em 2026-07 junto com os pre-checks do conftest), alem dos gates rapidos no HEAD final. O CI filtra `not lento`, entao regressoes nos lentos (e poluicao de isolamento que so aparece em sessao mista) sao invisiveis fora da maquina local — foi assim que o gotcha do sys.modules['ui'] ficou latente

- 2026-05-04 - `relatorio-vulnerabilidades.md` v2.1 (commit anterior em `main`) ja registra TODOS os 6 scans de seguranca como executados: bandit + pip-audit + gitleaks + trivy fs+image em 29/04/2026, SonarQube fechado em [#107], OWASP ZAP baseline em 02/05/2026 (0 FAIL / 2 WARN aceitos, fechado em [#108]). Entrada anterior 2026-04-27 ("5 scans pendentes") esta superada. Quando `MEMORY.md` conflitar com o estado atual do doc, o doc vence — esta entrada documenta a contradicao em vez de editar a historica
- 2026-05-04 - Workflow de PR: depois de aplicar fix de comentario de bot e responder a thread (accept+SHA), **resolver a thread** via GraphQL — sem isso o usuario nao tem sinal visual de que o feedback foi tratado. Listar threads: `gh api graphql -f query='query { repository(owner: "...", name: "...") { pullRequest(number: N) { reviewThreads(first: 50) { nodes { id isResolved comments(first: 1) { nodes { databaseId author { login } } } } } } } }'`. Resolver: `gh api graphql -f query='mutation { resolveReviewThread(input: { threadId: "PRRT_..." }) { thread { id isResolved } } }'`. Auto-resolver so threads de bot; threads de humanos o humano resolve. Convencao ja documentada em `postech-ai-helper/ai/canonical/pr-review-response.md`.
- 2026-05-04 - Coverage config vive em `.coveragerc` na raiz, NAO em `pyproject.toml` (corrige a entrada 2026-04-26 abaixo). `.coveragerc` define `fail_under=95`, `source=src,ui`, e `omit` para `ui/paginas/*`, `ui/componentes/*`, `ui/app.py`, `ui/__main__.py` (excluidos do gate unitario; cobertos por suite E2E). pyproject.toml so tem comentario apontando pra `.coveragerc` (coverage.py prefere `.coveragerc` quando ambos existem).
- 2026-05-04 - NiceGUI: dialog (ou qualquer overlay) criado dentro do handler `on_click` de um `ui.menu_item` precisa ser envolvido em `with context.client.layout:`. Razao: `events.handle_event` (em nicegui/events.py, linha ~425 na 2.x) ativa o slot do `parent_slot` do sender, que para o menu_item e o slot do `q-menu`. Como `MenuItem(..., auto_close=True)` registra `menu.close` como segundo handler, o batch enviado contem dialog-criado-dentro-do-menu + dialog.value=True + menu.value=False. O Vue/Quasar desmonta o `q-menu` (e o dialog filho junto) antes de renderizar — e o dialog so reaparece quando o menu reabre. O proprio NiceGUI usa esse escape para `ui.notification`/`ui.header`/`ui.drawer`/`ui.footer`. Helper compartilhado `confirmar` em `ui/componentes/dialogo_confirmacao.py` ja faz o wrap; chamadas inline (`_dialog_consentimento`, `_exportar_dados` em `ui/paginas/clientes.py`) tambem. Combinar com `dialog.on("hide", dialog.delete)` apos `dialog.open()` para que o dialog nao acumule no layout (ui.dialog so esconde no close, nao destroi).
- 2026-04-26 - Coverage configuration lives in `pyproject.toml` (not `.coveragerc`) -- look there for thresholds and exclusions
- 2026-04-26 - The `Makefile` uses `bash -c '...'` extensively in its rules -- on Windows it must run from Git Bash, not PowerShell
- 2026-04-26 - Per ADR-009 the codebase identifiers (entities, methods, variables) are written in Portuguese **without accents**; user-facing prose (READMEs, ADRs, setup guides) is in Portuguese with normal orthography

## Gotchas

- 2026-07-02 - Fixture `screen` do NiceGUI 3 (main_file) corrompe `sys.modules['ui']` apos rodar: deixa stub sem `__file__` no lugar do pacote do projeto e `monkeypatch.setattr("ui...")` de testes seguintes na MESMA sessao falha com AttributeError (13 testes). Guard: fixture autouse `_preservar_sys_modules_ui` em tests/unitarios/ui/conftest.py (snapshot/restore) + test_isolamento_modulos.py

- 2026-06-12 - [PR #22] `_log.info(...)` de src/ NAO aparece no stdout do container: structlog usa stdlib LoggerFactory e o root logger fica no default WARNING (nenhum basicConfig/handler no boot) — so warning+ sai (via lastResort do stdlib). Nao valide feature por log de info; confirme por efeito observavel (ex.: API do Jaeger) ou logue warning
- 2026-06-12 - [PR #17] `structlog.testing.capture_logs` NAO intercepta logger module-level (`_log = structlog.get_logger(__name__)`) que ja foi usado depois de um boot do app: `configurar_logging` usa `cache_logger_on_first_use=True`, entao quando a suite de integracao (roda antes de `unitarios/` na ordem alfabetica) exercita o caminho que loga, o proxy cacheia os processors de PRODUCAO e o capture dos testes unitarios ve lista vazia — falha so na suite completa, passa em isolamento. Correcao padrao: fixture autouse no arquivo de teste monkeypatchando o `_log` do modulo por um proxy novo (`structlog.get_logger("test_x")`) — ver `test_notificacoes.py`/`test_dispatcher.py`
- 2026-05-03 - [in-flight from PR #139] `make ghcr-prune` no Git Bash do Windows precisa de DOIS workarounds simultaneos: (a) `MSYS_NO_PATHCONV=1` no `bash -c` envolvente -- senao MSYS reescreve `/user`, `/users/...` em arg do `gh.exe` (binario Windows nativo) pra `C:/Program Files/Git/user`, e a Github API rejeita; e (b) `| tr -d "\r"` entre `jq -r ...` e `while read -r id` -- senao o id captura `\r` final (gh.exe e jq.exe emitem CRLF) e a URL DELETE seguinte vira `.../versions/838326537%0D` que Go net/url rejeita ("invalid control character in URL"). Aplicaveis a qualquer chamada de binario Windows nativo no Makefile que receba paths Unix-like ou cujo stdout vire input de `read`. Tambem: o pipeline `update-ghcr` agora patcha `db-image/docker-compose.yml` ANTES do prune e trata prune como best-effort (`|| echo "..."`), garantindo consistencia compose<->GHCR mesmo se o prune falhar -- se o patch viesse depois e o prune morresse com `set -e`, a NEW_KEY ficava perdida no scope bash e nao havia recovery automatico
- 2026-05-03 - [in-flight from PR #139] `docker compose up` retorna assim que o entrypoint do container dispara, MUITO antes do uvicorn realmente atender. Resultado: porta 8000 ja aberta (Docker proxy escutando) mas browser leva `ERR_EMPTY_RESPONSE` por dezenas de segundos -- piora drasticamente sob emulacao QEMU (~40s observado em maquina amd64) quando imagens publicadas em arm64 rodam emuladas. Solucao em `db-image/docker-compose.yml`: healthcheck no `app` probando `/api/v1/saude` via Python+urllib (NAO `curl`/`wget` -- podem nao estar na imagem slim do FastAPI), `start_period: 30s` pra cobrir QEMU sem marcar unhealthy, `ui.depends_on.app.condition: service_healthy` (era `service_started`), e o QUICKSTART agora documenta `docker compose up -d --wait` pra terminal segurar ate todos os healthchecks passarem. Sem o healthcheck, `--wait` nao tem o que esperar e o ganho some
- 2026-05-01 - [in-flight from PR fix/all] PostgreSQL serializa DELETE em uma tabela referenciada via FK e INSERT na tabela referenciadora atraves do conflito ``FOR UPDATE`` × ``FOR KEY SHARE``: a validacao de FK em INSERT pega ``FOR KEY SHARE`` na linha referenciada, que conflita com o ``FOR UPDATE`` adquirido pelo lado do DELETE. Em `RemoverVeiculo`, `ClienteRepository.bloquear_veiculo_para_remocao(veiculo_id)` (`select(...).with_for_update()` em `veiculos.id`) fecha a janela de corrida com criacao concorrente de OS — sem precisar mexer no caminho de criacao. O lock e silent no-op se a linha nao existir; o caller ja validou a presenca no agregado
- 2026-04-29 - Em macOS com Colima, testcontainers (suite de integracao) precisa de duas variaveis pra achar o socket: `DOCKER_HOST=unix:///Users/<user>/.colima/default/docker.sock` (o cliente docker) e `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock` (o path que o testcontainers passa pra dentro do ryuk container). Sem o override do socket, ryuk tenta montar o path literal do host (`.colima/default/...`) que nao existe dentro da VM e o teste falha com "operation not supported"
- 2026-04-29 - SQLAlchemy ORM imperative mapping: o evento `load` so dispara no PRIMEIRO carregamento de uma instancia. Apos `session.expire(obj)` + re-fetch na mesma sessao, o evento que dispara e `refresh` (assinatura diferente: `target, context, attrs`). Listeners que reidratam VOs precisam estar registrados em ambos — `cliente_veiculo/infraestrutura/mapping.py` ilustra o padrao com `_reidratar_documento` chamado por `_reconstruir_documento_on_load` e `_reconstruir_documento_on_refresh`. Sem o listener de refresh, `repository.anonimizar_dados` + `session.expire_all()` + `obter_por_id` retorna o documento ANTIGO em memoria
- 2026-04-26 - On Git Bash (MSYS2) absolute Unix-style paths (`/tmp/foo`) passed as args to native Windows binaries (e.g. `docker.exe`) are auto-translated to Windows paths (`C:/Users/.../Temp/foo`); the Makefile prefixes `MSYS_NO_PATHCONV=1` on `docker compose cp/exec` lines that pass `/tmp/...` to keep the path literal inside the container. Without it, `make seed-users-docker` and `make reset-db` fail with `No such file or directory: /app/C:/Users/...` from python in the container
- 2026-04-26 - The canonical workspace root is `~/git/fiap/postech-sw-architecture/` (per `postech-ai-helper/ai/canonical/workspace-structure.md`); cloning directly under `~/git/fiap/` still works for sibling-relative pointers, but the helper's `setup.sh` exits non-zero when run from a non-canonical workspace, and on Windows symlink creation requires Developer Mode regardless
- 2026-04-26 - On Windows, the project's Docker socket detection script must detect the named pipe (`\\.\pipe\docker_engine`) instead of probing only Unix sockets; the MINGW/MSYS/CYGWIN branch in `scripts/docker-check.sh` (added in PR #81) resolves this
- 2026-04-26 - Server-side render of NiceGUI `@ui.page` handlers runs before the client websocket connects; any handler-time HTTP call that writes to `app.storage.tab` raises `RuntimeError` unless the storage adapter is defensive (returns None and no-ops on writes when the backend is unreachable)

## Tech debt / TODO

- 2026-06-12 - MEDIUM - [PR #22] Traces do FastAPI capturam a query string completa: GET /api/v1/acompanhamento?placa=..&documento=.. poe CPF/placa em claro nos atributos http do span. Risco aceito no escopo I11 (Jaeger da demo e in-memory/efemero, flag default off, ADR-020 escopo minimo). Follow-up se OTel passar da demo: server_request_hook sobrescrevendo url.query/http.target, ou tirar documento da query string do canal publico
- 2026-04-26 - LOW - HTTP probes during initial render of a NiceGUI page should be deferred to a `client.on_connect` handler (or equivalent) so they execute with a client context instead of as side effects of import/render

## Code Quality (CodeQL) — gate pre-PR
Antes de abrir um PR que ALTERA codigo Python, rode `make codeql-quality` e garanta **0 findings ativos** (o script sai != 0 se sobrar). Para cada finding novo: (1) corrija se for bug real; (2) suprima na linha com `# codeql[<regra>]` + razao se for FP pontual; (3) desabilite a regra em `.github/codeql/codeql-config.yml` (`query-filters`) ou ignore o path (`paths-ignore`) se a regra nao fizer sentido pro projeto. Nao abra PR com o gate vermelho. Detalhes: `scripts/codeql_quality.py`, `scripts/README.md`.
