# Dívida Técnica

> [↑ Raiz do projeto](../../README.md)

> **Versão**: 3.4 (2026-07-04) — **TD-034 fechado** (issue #180): o acompanhamento publico virou `POST` com placa/documento no corpo, tirando a PII da URL (query string -> body); UI, collection e full-test acompanham, teste de mesa confirma `POST` 200 / `GET`-com-PII 405. Contagens: *Itens Resolvidos* (29) / *Itens Abertos* (5). Versão 3.3 (2026-07-03) — registrado **TD-034** (risco aceito: CPF/CNPJ em query param GET do acompanhamento publico — finding da revisao multi-perspectiva da entrega; mitigacoes e racional na linha). Contagens: *Itens Resolvidos* (28) / *Itens Abertos* (6). Versão 3.2 (2026-07-02) — fechados **TD-032** (fixture autouse `_reset_encryption_singleton` no conftest raiz — issue #127) e **TD-033** (varredura dos `raise ValueError`: placa deixou de ecoar o valor, guard tests `TestMensagemSemPii`; eco de `str(exc)` no handler mantido por decisão — issue #126). Na mesma leva: scrubber de telefone cobre formatos sem espaço/+55 corrido + denylist telefone/celular/phone/contato (issue #99) e guard de `sys.modules['ui']` contra a poluição do Screen do NiceGUI 3 (13 testes quebravam em suíte local mista; CI com `-m "not lento"` não via). Contagens: *Itens Resolvidos* (28) / *Itens Abertos* (5). Versão 3.1 (2026-07-01) — auditoria de finalização da entrega ([issue #128](https://github.com/fiap-postech-sw-architecture/postech-sw-arch-p2/issues/128)): registrados **TD-032** (order-dependence latente do singleton `EncryptionService` nos testes — issue #127) e **TD-033** (mensagens de `ValueError` ecoadas cruas no corpo 422 — issue #126) como *Itens Abertos*. Os **bugs confirmados** da mesma auditoria viraram issues no GitHub (#117–#122), não entram neste ledger (regra da v2.8); o plano de ataque da finalização vive em [finalization-plan.md](../entrega/fase2/finalization-plan.md). Higiene do tracker na mesma auditoria: issues **#83/#35/#33/#31** (resolvidas em main pelos PRs #101/#50/#64/#62, esquecidas abertas) fechadas com evidência, e **#75** fechada pelo merge do **PR #116** — gates reais de segurança no CI (`security.yml`: pip-audit/gitleaks/trivy + Dependabot; bandit ampliado a `relay`/`scripts`; CodeQL via default setup), que também levou os upgrades de 5 CVEs (cryptography/starlette/fastapi) à main. Contagens: *Itens Resolvidos* (26) / *Itens Abertos* (7). Versão 3.0 (2026-06-30) — fechados os **deferidos** da campanha: **#72** (PR #112 — erasure LGPD cascateia para veículos: migração 006 alarga `placa` + sentinela `PlacaAnonimizada`), **#80** (PR #110 — orçamento complementar cobra trabalho extra em EM_EXECUCAO + reserva de estoque) e **TD-027** (PR #114 — webhook de orçamento assinado por HMAC). Cada um com gate local completo + review single-shot + teste de mesa, merge `--admin` (CI billing-blocked). Restam abertos só os TDs originais da fase 1 (TD-002/004/006/013/014); follow-ups abertos: #111 (semântica de rejeição do complementar) e #113 (pre-request script HMAC no Postman). Contagens: *Itens Resolvidos* (26) / *Itens Abertos* (5). Versão 2.9 (2026-06-30) — campanha de limpeza pós-auditoria (Tier 3+4): 7 TDs fechados — **TD-028**/**TD-029** (PR #105: pré-hash bcrypt 72 bytes + refresh não vale como access), **TD-031** (PR #106: `--cov-fail-under=95` explícito), **TD-025** (PR #107: índice B-tree em `itens_da_ordem.item_estoque_id`), **TD-024** (PR #108: `securityContext` nos workloads k8s + UID pinado, teste de mesa kind), **TD-026** (decisão: deploy fora do pipeline não-suportado, doc em [k8s/README.md](../../k8s/README.md)), **TD-030** (decisão: eventos órfãos documentados em [eventos-de-dominio.md](../arquitetura/eventos-de-dominio.md)). **TD-027** (webhook HMAC) **deferido** — o esquema atual é MVP documentado (ADR-021) e a evolução exige ripple no caller (e2e + postman da entrega + ADR) às vésperas da entrega. Tier 3 (issues GitHub) também fechado: #73 (ENCRYPTION_KEY guard + decrypt sem fail-open), #76 (auditoria LGPD + erasure admin-only); #72 (cascade de veículo) deferido com plano. Contagens passam a *Itens Resolvidos* (25) / *Itens Abertos* (6). Versão 2.8 (2026-06-28) — auditoria pré-entrega da fase 2 (teste de mesa profundo no kind + review estruturado em 6 dimensões, relatório em [auditoria-pre-entrega-fase2.md](auditoria-pre-entrega-fase2.md)) registrou **TD-024..TD-031** como *Itens Abertos* — 8 compromissos **aceitos/justificados** (não bugs): TD-024 (`securityContext` ausente nos workloads k8s), TD-025 (sem índice em `itens_da_ordem.item_estoque_id`), TD-026 (ordenação migração-antes-do-rollout não auto-enforçada pelo cluster), TD-027 (webhook de orçamento sem HMAC), TD-028 (bcrypt trunca em 72 bytes), TD-029 (refresh token aceito como access — claim `type` não checado), TD-030 (eventos de domínio órfãos — intenção de modelagem sem consumidor na fase 2), TD-031 (gate de cobertura de `src/` implícito via `.coveragerc`). Os **bugs confirmados** e as **correções delivery-facing** da mesma auditoria viram **issues no GitHub**, não entram neste ledger. Contagens passam a *Itens Resolvidos* (18) / *Itens Abertos* (13). Versão 2.7 (2026-06-28) — TD-010 fechado por decisão (PR #71): SonarQube/SonarCloud **não será gate de CI** — repo privado (SonarCloud pago) + self-hosted exige servidor desproporcional ao MVP; a análise estática em CI é CodeQL (`make codeql-quality`) + ruff + bandit. SonarQube permanece scan manual de fechamento de fase, suportado pelo `sonar-project.properties` (mantido na raiz, foi o que rodou no fechamento da fase 1, issue #107). Decisão registrada em [ADR-011](../arquitetura/adr/011-pipeline-seguranca-analise-estatica.md); contagens passam a *Itens Resolvidos* (18) / *Itens Abertos* (5). Versão 2.6 (2026-06-28) — TD-007 fechado (PR #70): `Contato` Value Object ([contato.py](../../src/cliente_veiculo/dominio/contato.py)) — texto livre validado (não-vazio, `<=255`, `strip`, `__repr__` PII-safe) — substitui o `contato: str` primitivo no agregado `Cliente`. Persiste na mesma coluna `String(255)` via shadow `_contato_valor` + event listeners (load/refresh reidrata o VO; before_insert/before_update serializa de volta), espelhando o padrão CPF/Placa — **sem migração**. Aceita o sentinela LGPD `anonimizado@anonimizado.local` gravado pelo raw UPDATE de `anonimizar_dados` (texto livre não-vazio). `email()` **não** adicionado ao VO: o handler de notificação (`ordem_servico.aplicacao.notificacoes`) consome o contato como `str` cru via `ClientePort` (cross-context por porta), e mover a regex para o VO exigiria o primeiro import `ordem_servico.aplicacao → cliente_veiculo.dominio` do código — quebraria o isolamento de bounded context; o `_extrair_email` segue em `notificacoes.py`. Contagens passam a *Itens Resolvidos* (17) / *Itens Abertos* (6). Versão 2.5 (2026-06-28) — TD-005 fechado (PR #68): coluna `orcamento_json` migrada de `Text` para `jsonb` nativo (migração [004](../../migrations/versions/004_orcamento_jsonb.py)), removendo a camada manual `json.dumps`/`json.loads` no [mapping.py](../../src/ordem_servico/infraestrutura/mapping.py); a serialização passa a seguir o padrão de `outbox.payload` (dict cru passado à coluna `JSONB().with_variant(JSON(), "sqlite")`). Sem índice GIN (nenhuma consulta filtra por conteúdo do orçamento — YAGNI); contagens passam a *Itens Resolvidos* (16) / *Itens Abertos* (7). Versão 2.4 (2026-06-28) — TD-023 fechado (PR #67): rate-limit por IP real do cliente atrás de proxy — `ProxyHeadersMiddleware` do uvicorn aplicado programaticamente em `criar_app` ([src/main.py](../../src/main.py)) quando `TRUSTED_PROXIES` está configurado (`configurar_proxy_headers`/`_resolver_trusted_proxies` em [middleware.py](../../src/compartilhado/interfaces/middleware.py)), reescrevendo `request.client` a partir do `X-Forwarded-For` confiável ANTES do `SlowAPIMiddleware` ler a chave; default vazio (não confia em XFF, sem spoof) ([ADR-023](../arquitetura/adr/fase2/023-rate-limiter-storage-compartilhado.md)); contagens passam a *Itens Resolvidos* (15) / *Itens Abertos* (8). Versão 2.3 (2026-06-28) — TD-022 fechado (PR #66): relay instrumentado com métricas OTel expostas via Prometheus — `MeterProvider` + `PrometheusMetricReader` ([relay/metrics.py](../../relay/metrics.py)) servem `/metrics` (porta 9100), scrapeado pelo [`k8s/prometheus.yaml`](../../k8s/prometheus.yaml) (Service `pytstop-relay-metrics`): gauges de profundidade (pendentes/idade/dead, mesma query do gauge structlog) + contadores entregue/falha/dead/retry ([ADR-024](../arquitetura/adr/fase2/024-metricas-prometheus.md)); contagens passam a *Itens Resolvidos* (14) / *Itens Abertos* (9). Versão 2.2 (2026-06-28) — TD-021 fechado (PR #66): fencing de lease na entrega — re-lock `FOR UPDATE SKIP LOCKED` + checagem de status `pendente` na transação por-linha (`bloquear_para_entrega` em [relay/processador.py](../../relay/processador.py)), serializando réplicas concorrentes sem duplicar a entrega → torna `replicas>1` seguro sem mudança de schema ([ADR-022](../arquitetura/adr/fase2/022-transactional-outbox-relay.md)); contagens passam a *Itens Resolvidos* (13) / *Itens Abertos* (10). Versão 2.1 (2026-06-27) — TD-011 fechado (PR #65): OWASP ZAP baseline automatizado no [full-test-ci](../../.github/workflows/full-test-ci.yml) (DAST contra a stack compose, gate com `.zap/rules.tsv`), relatório publicado como artefato + alvo `make dast` para paridade local ([ADR-011](../arquitetura/adr/011-pipeline-seguranca-analise-estatica.md)); contagens passam a *Itens Resolvidos* (12) / *Itens Abertos* (11). Versão 2.0 (2026-06-27) — TD-015 fechado (PR #64): migração movida do entrypoint do pod para um Job dedicado (`pytstop-migrate`, [`k8s/jobs/migration-job.yaml`](../../k8s/jobs/migration-job.yaml)) aplicado antes do rollout com `kubectl wait --for=condition=complete`; `RUN_MIGRATIONS_ON_STARTUP`/`RUN_SEED_ON_STARTUP` passam a `false` no cluster → resolve a corrida de migração com N réplicas ([ADR-019](../arquitetura/adr/fase2/019-pipeline-cicd-deploy.md)); contagens passam a *Itens Resolvidos* (11) / *Itens Abertos* (12). Versão 1.9 (2026-06-27) — TD-023 aberto: rate-limit keyed pelo IP do *peer* imediato (`get_remote_address`/`request.client.host`); sem `X-Forwarded-For` confiável (+ uvicorn `--proxy-headers`), atrás de ingress todo o tráfego externo colapsa num único bucket ([ADR-023](../arquitetura/adr/fase2/023-rate-limiter-storage-compartilhado.md)); contagens passam a *Itens Resolvidos* (10) / *Itens Abertos* (13). Versão 1.8 (2026-06-27) — TD-016 fechado (PR #62): rate limiter slowapi com backend compartilhado via Redis (`storage_uri`, env `RATE_LIMIT_STORAGE_URI`, fallback in-memory) → limite correto sob HPA, fechando a metade do rate limiter da RNF-024 ([ADR-023](../arquitetura/adr/fase2/023-rate-limiter-storage-compartilhado.md)); contagens passaram a *Itens Resolvidos* (10) / *Itens Abertos* (12). Versão 1.7 (2026-06-27) — reconciliação com o código: TD-009 fechado (eventos `ClienteCadastrado`/`ServicoCadastrado` implementados e emitidos via `_registrar_evento`); TD-007 reescrito (validação de dígito/formato via brutils já entregue — remanescente é só telefone/e-mail sem VO próprio); TD-010 nota o gate CodeQL local; estrutura agora separa explicitamente *Itens Resolvidos* (9) de *Itens Abertos* (13, em 4 grupos). Versão 1.6 — TD-008 resolvido (Transactional Outbox/RF-018, PR #56); adicionados TD-021 (relay HA/fencing) e TD-022 (observabilidade do relay). Versão 1.5: TD-018 fechado por remoção do `db-image/` (fast-check da fase 1) do repo da fase 2. Versão 1.4: dep `mutmut` removido (3.x quebrado); TD-006 sem tooling. Versão 1.3: TD-019 fechado (PR #50). Versão 1.2 (2026-06-22): reconciliação com o código (TD-003/TD-017 fechados, TD-002/004/005/008/009/016 corrigidos).

Simplificações deliberadas cujo custo de correção é aceito para o escopo do MVP.

Funcionalidades que não serão implementadas no MVP estão classificadas como Could Have no [PRD](../requisitos/prd.md). Requisitos que serão implementados estão nos respectivos RFs em [requisitos.md](../requisitos/requisitos.md).

Classificação por tipo:

- **Deliberado**: assumido conscientemente pela equipe para acelerar entrega ou validar hipóteses
- **Acidental**: surge sem que a equipe perceba, por desconhecimento ou mudanças inesperadas
- **Planejado**: equipe sabe que a solução não é ideal, documenta e planeja pagar depois
- **Negligenciado**: débito ignorado por muito tempo, mesmo após identificação

> 📋 **Plano de ataque** (snapshot histórico da campanha de junho) — priorização e como resolver cada TD: **[plano-ataque.md](plano-ataque.md)**. O status vivo é a contagem deste README (6 abertos); a regra segue valendo: cada TD vira **um PR próprio** que atualiza **todos os docs afetados no mesmo PR**.

> 📋 Auditoria pré-entrega (2026-06-28): [auditoria-pre-entrega-fase2.md](auditoria-pre-entrega-fase2.md) — achados priorizados do teste de mesa profundo + review estruturado.

## Itens Resolvidos (29)

| # | Área | Descrição | Resolução |
|---|---|---|---|
| TD-034 | Seguranca/LGPD | Acompanhamento publico recebia CPF/CNPJ como query parameter GET (PII na URL) | **Fechado** (issue #180) — o endpoint virou `POST /api/v1/acompanhamento` com placa/documento no CORPO; a consulta segue somente-leitura e rate-limited, mas a PII deixa de aparecer em access log de proxy/ingress, historico de browser e logs de client HTTP. UI, collection Postman e full-test acompanham; teste de mesa confirma `POST` 200 e `GET` com PII na URL -> 405. |
| TD-001 | Segurança | Sem mecanismo de consentimento explícito LGPD | **Fechado** — Implementado no MVP via RF-019: endpoints `POST/DELETE /clientes/{id}/consentimento` com entidade `ConsentimentoCliente`. |
| TD-020 | UI | Listagem da `ui/` não mostrava OS encerradas por default pós RF-023 | **Fechado** — PR #28: a `ui/` ganhou o toggle "Mostrar encerradas" (passa `incluir_encerradas`), os badges passam a exibir `situacao` (RF-021) e o dialog de nova OS aceita serviços/peças inline (RF-020). |
| TD-012 | Segurança | Sem SBOM automatizado no CI (geração manual) | **Fechado** — job `sbom` no [ci.yml](../../.github/workflows/ci.yml) gera o SBOM CycloneDX a partir do lockfile a cada run e publica como artefato; alvo `make sbom` para geração local ([ADR-012](../arquitetura/adr/012-licenciamento-software-sbom.md)). |
| TD-003 | Infra | Sem CSP headers (Content-Security-Policy) | **Fechado** — `SecurityHeadersMiddleware` aplica `Content-Security-Policy: default-src 'none'` em toda resposta (exceto as rotas de docs do Swagger/ReDoc, que precisam de inline scripts), além de `X-Frame-Options: DENY`, HSTS, `X-Content-Type-Options: nosniff` e `Cache-Control: no-store` ([middleware.py](../../src/compartilhado/interfaces/middleware.py)); coberto por `tests/unitarios/test_security.py` e `tests/unitarios/compartilhado/test_middleware.py`. |
| TD-017 | Observabilidade | Traces capturavam a query string (CPF/placa) — PII no OTel | **Fechado** (#34) — `_redigir_pii_da_span` (server_request_hook do `FastAPIInstrumentor`) redige `url.query` e remove a query de `http.target`/`url.path` antes do export dos spans ([observability.py](../../src/compartilhado/infraestrutura/observability.py)). |
| TD-019 | Arquitetura | `aplicacao → infraestrutura` na autenticação fora do contrato forbidden | **Fechado** (#50) — `PasswordHasherPort` + `JWTServicePort` em [aplicacao/ports.py](../../src/autenticacao/aplicacao/ports.py) (Protocol); a infra implementa e o composition root injeta por DI. O contrato `forbidden` do import-linter passou a proibir `aplicacao → infraestrutura` em todos os contextos, verificado por `make lint-arch`/CI (RNF-017). |
| TD-018 | Infra | `db-image/` no GHCR ainda com imagens da fase 1 | **Fechado** — `db-image/` (fast-check da fase 1) removido do repo da fase 2: confundia (imagens `-p1` sem RF-020..024/Mailpit) e não agregava (nada usa; compose e testes usam `postgres:16` vanilla, app builda do fonte e o CD publica `-p2-app` por SHA). Caminhos oficiais: `make up`, k8s/CD. |
| TD-008 | Domínio | Dispatch de domain events síncrono e in-process (sem outbox) | **Fechado** (2026-06-25, PR #56) — Resolvido via RF-018 (Transactional Outbox): a UoW grava `IntegrationEvents` na tabela `outbox` na mesma transação da OS; o relay (`python -m relay`) implementa claim-then-deliver com head-of-line, backoff/DLQ e idempotência via `processed_events`. Notificação proativa via `LISTEN/NOTIFY` (PostgreSQL). Detalhes em [ADR-022](../arquitetura/adr/fase2/022-transactional-outbox-relay.md). |
| TD-009 | Domínio | Dois eventos de criação do event storming sem classe nem emissão | **Fechado** (PR #48) — `ClienteCadastradoEvent` ([events.py](../../src/cliente_veiculo/dominio/events.py)) e `ServicoCadastradoEvent` ([events.py](../../src/catalogo_servicos/dominio/events.py)) implementados e emitidos via `_registrar_evento` nas factories `Cliente.criar`/`ServicoOferecido.criar`; cobertos por testes unitários (`test_cliente.py`, `test_servico_oferecido.py`). |
| TD-016 | Infra | Rate limiter slowapi in-memory por pod | **Fechado** (PR #62) — backend compartilhado via Redis (`storage_uri`): Deployment+Service no [k8s](../../k8s/redis.yaml), env `RATE_LIMIT_STORAGE_URI`, fallback in-memory; limite correto sob HPA (RNF-024). [ADR-023](../arquitetura/adr/fase2/023-rate-limiter-storage-compartilhado.md). |
| TD-015 | Infra | Corrida de migração com múltiplas réplicas (alembic no entrypoint) | **Fechado** (PR #64) — migração movida do entrypoint para um Job dedicado [`k8s/jobs/migration-job.yaml`](../../k8s/jobs/migration-job.yaml) aplicado antes do rollout (`kubectl wait --for=condition=complete`); resolve a corrida com N réplicas. [ADR-019](../arquitetura/adr/fase2/019-pipeline-cicd-deploy.md). #33. |
| TD-011 | Segurança | Sem DAST automatizado (OWASP ZAP manual) | **Fechado** (PR #65) — ZAP baseline automatizado no [full-test-ci](../../.github/workflows/full-test-ci.yml) (DAST contra o compose), relatório como artefato + `make dast` local; regras dos 2 WARNs aceitos em [`.zap/rules.tsv`](../../.zap/rules.tsv). [ADR-011](../arquitetura/adr/011-pipeline-seguranca-analise-estatica.md). |
| TD-021 | Infra | Relay sem fencing de lease para `replicas>1` | **Fechado** (PR #66) — fencing de lease na entrega: re-lock `FOR UPDATE SKIP LOCKED` + checagem de status na tx por-linha (`bloquear_para_entrega` em [relay/processador.py](../../relay/processador.py)), tornando `replicas>1` seguro sem duplicar entrega; sem mudança de schema. [ADR-022](../arquitetura/adr/fase2/022-transactional-outbox-relay.md). |
| TD-022 | Observabilidade | Relay sem métricas OTel nem alerting | **Fechado** (PR #66) — relay instrumentado com métricas OTel expostas via Prometheus ([relay/metrics.py](../../relay/metrics.py), `MeterProvider`+`PrometheusMetricReader`, /metrics scrapeado pelo [`k8s/prometheus.yaml`](../../k8s/prometheus.yaml)): outbox pendentes/idade/dead + contadores entregue/falha/dead/retry. [ADR-024](../arquitetura/adr/fase2/024-metricas-prometheus.md). |
| TD-023 | Segurança | Rate-limit keyed pelo IP do peer imediato (sem X-Forwarded-For confiável) | **Fechado** (PR #67) — rate-limit por IP real do cliente atrás de proxy: `ProxyHeadersMiddleware` do uvicorn aplicado quando `TRUSTED_PROXIES` está configurado, reescrevendo `request.client` a partir do XFF confiável; default vazio (não confia em XFF, sem spoof). [ADR-023](../arquitetura/adr/fase2/023-rate-limiter-storage-compartilhado.md). |
| TD-005 | Domínio | Orçamento em coluna Text, sem consulta estruturada nem índice | **Fechado** (PR #68) — coluna `orcamento_json` migrada de `Text` para `jsonb` nativo (migração [004](../../migrations/versions/004_orcamento_jsonb.py)), removendo a camada manual `json.dumps`/`json.loads` no [mapping.py](../../src/ordem_servico/infraestrutura/mapping.py); a serialização segue o padrão da `outbox.payload` (dict cru passado à coluna `JSONB().with_variant(JSON(), "sqlite")`). Sem índice GIN (nenhuma consulta por conteúdo do orçamento — YAGNI). |
| TD-007 | Domínio | Contato do cliente como primitivo, sem Value Object dedicado | **Fechado** (PR #70) — `Contato` Value Object ([contato.py](../../src/cliente_veiculo/dominio/contato.py)): texto livre validado (não-vazio, `<=255` chars, `strip`, `__repr__` PII-safe) substitui o `contato: str` primitivo no agregado `Cliente`. Persiste na mesma coluna `String(255)` via shadow `_contato_valor` + event listeners (load/refresh reidrata; before_insert/before_update serializa), padrão CPF/Placa — **sem migração**. Aceita o sentinela LGPD `anonimizado@anonimizado.local` (texto livre não-vazio) gravado pelo raw UPDATE de `anonimizar_dados`. `email()` deliberadamente **não** adicionado ao VO: o handler `notificacoes.py` consome o contato como `str` cru via `ClientePort` (cross-context por porta) e mover a regex para o VO criaria o primeiro import `ordem_servico.aplicacao → cliente_veiculo.dominio` do código, quebrando o isolamento de bounded context — o `_extrair_email` segue em `notificacoes.py`. |
| TD-010 | Segurança | SonarQube não integrado como gate de CI | **Resolvido por decisão** (PR #71) — SonarQube/SonarCloud não será gate de CI: repo privado (SonarCloud pago) + self-hosted exige servidor desproporcional ao MVP; a análise estática em CI é CodeQL (`make codeql-quality`) + ruff + bandit. SonarQube permanece scan manual de fechamento de fase (suportado pelo `sonar-project.properties`, mantido). Decisão registrada em [ADR-011](../arquitetura/adr/011-pipeline-seguranca-analise-estatica.md). |
| TD-028 | Segurança | bcrypt trunca a senha em 72 bytes (pwdlib sem pré-hash) | **Fechado** (PR #105) — `hash_senha` pré-hasha `base64(sha256(senha))` (44 bytes) antes do bcrypt, eliminando a colisão de prefixo de 72 bytes (padrão `bcrypt_sha256`). `verificar_senha` cai para o esquema legado (senha crua) **só** quando a senha cabe em 72 bytes (compat de migração; reseed no deploy extingue os legados). [password_hasher.py](../../src/autenticacao/infraestrutura/password_hasher.py). |
| TD-029 | Segurança | Refresh token aceito como access (claim `type` não checado) | **Fechado** (PR #105) — `obter_usuario_atual` ([middleware.py](../../src/autenticacao/interfaces/middleware.py)) exige `type == "access"` → 401 caso contrário, espelhando o check `type == "refresh"` do fluxo de refresh. Defense-in-depth além do RBAC. |
| TD-031 | Qualidade/CI | Gate de cobertura de `src/` implícito via `.coveragerc` | **Fechado** (PR #106) — `--cov-fail-under=95` explícito no step de cobertura de `src/` no [ci.yml](../../.github/workflows/ci.yml), alinhado ao `.coveragerc` e ao step de UI. |
| TD-025 | Dados/Performance | Sem índice em `itens_da_ordem.item_estoque_id` | **Fechado** (PR #107) — migração [005](../../migrations/versions/005_indice_item_estoque.py) (reversível) cria o índice B-tree `ix_itens_da_ordem_item_estoque_id`; a declaração `Index` no [mapping.py](../../src/ordem_servico/infraestrutura/mapping.py) mantém o `create_all` em sync. Teste de mesa: `EXPLAIN` confirma `Index Scan` no filtro de `existe_ativa_com_item_estoque`. |
| TD-024 | Infra/Segurança | `securityContext` ausente nos workloads k8s | **Fechado** (PR #108) — pod+container `securityContext` (`runAsNonRoot`, `runAsUser/runAsGroup/fsGroup 1001`, `seccompProfile:RuntimeDefault`, `allowPrivilegeEscalation:false`, `readOnlyRootFilesystem`, `capabilities.drop:[ALL]`) + `emptyDir` `/tmp` nos 3 workloads próprios (api/relay/migrate); UID/GID 1001 pinados no Dockerfile. Imagens de terceiros ficam fora (rodam como root). Teste de mesa kind: pods `Running` sob o hardening, `/saude` 200, heartbeat em `/tmp`. |
| TD-026 | Infra | Ordenação migração-antes-do-rollout não auto-enforçada | **Resolvido por decisão** — documentado em [k8s/README.md](../../k8s/README.md): deploy fora do pipeline (`cd.yml`/`make k8s-up`/`make cd-local`) é **não-suportado**; a ordem migração→rollout é garantida só pelo caminho imperativo. Auto-enforce (initContainer/sync-wave) fica para um eventual fluxo GitOps. |
| TD-030 | Arquitetura | Eventos de domínio órfãos (sem consumidor nem persistência) | **Resolvido por decisão** — documentado em [eventos-de-dominio.md](../arquitetura/eventos-de-dominio.md): os 8 `DomainEvent` de catálogo/cliente_veiculo/estoque são intenção de modelagem **sem consumidor na fase 2** (não bug); o caminho de promoção a `IntegrationEvent` (durabilidade via outbox) está descrito. |
| TD-027 | Segurança | Webhook de orçamento sem assinatura HMAC | **Fechado** (PR #114) — **assinatura HMAC por requisição** ([webhook_signature.py](../../src/compartilhado/infraestrutura/webhook_signature.py) + `validar_assinatura_webhook`): chave = `ORCAMENTO_WEBHOOK_TOKEN` (não trafega), HMAC-SHA256 de `{ordem_id}.{timestamp}.` + body, janela ±5min. **Limita** replay (residual dentro da janela aceito) e fecha adulteração. ADR-021 emendada; harness full-test migrado. Pre-request script do Postman → follow-up #113. |
| TD-032 | Testes | Singleton `EncryptionService._instance` compartilhado entre testes (order-dependence latente) | **Fechado** (issue #127) — fixture autouse `_reset_encryption_singleton` no `tests/conftest.py` raiz zera `_instance` antes de cada teste (padrão do `_reset_rate_limiter`); resets manuais do `test_encryption.py` removidos. Suíte passa isolada e completa. |
| TD-033 | Segurança | Handler 422 ecoa `str(exc)` de `ValueError` cru na resposta | **Fechado** (issue #126) — escolha registrada: **mensagens de domínio com rótulo fixo, sem interpolação de PII** (o eco de `str(exc)` no handler permanece, decisão handler-only de 2026-04-29). Varredura dos 75 `raise ValueError` de `src/`: única interpolação de PII era a placa ([placa.py](../../src/cliente_veiculo/dominio/placa.py), corrigida para `"Placa invalida"`); CPF/CNPJ já usavam rótulo fixo. Guard tests (`TestMensagemSemPii`) em test_placa.py e test_cpf.py bloqueiam regressão. Interpolações restantes auditadas: números/quantias/nomes de catálogo — não-PII. |

## Itens Abertos (5)

Débitos ativos, agrupados por área. Simplificações deliberadas cujo custo de correção é aceito no escopo do MVP.

### Geral

| # | Área | Descrição | Tipo | Severidade | Impacto no Negócio | Risco de Produção | Tendência de Crescimento | Justificativa |
|---|---|---|---|---|---|---|---|---|
| TD-002 | Domínio | Sem histórico de orçamentos (snapshot único substitui o anterior) | Deliberado | Baixa | Baixo | Não | Estável | Orçamento é um Value Object imutável persistido como snapshot único na coluna `orcamento_json` — cada novo orçamento substitui o anterior, sem versionamento nem timestamp. Funcionalidade parcial aceita. RF-017 (Could Have). |
| TD-004 | API | Sem notificações push/SMS (e-mail já implementado) | Deliberado | Baixa | Baixo | Não | Estável | RF-024 entregou notificação real por **e-mail** (`EmailPort` + `SmtpEmailAdapter`; Mailpit no compose). Push e SMS seguem fora do escopo do MVP; a inversão de dependência ([ADR-003](../arquitetura/adr/003-arquitetura-ddd-onion.md)) permite adicioná-los sem tocar no domínio. |
| TD-006 | Testes | Mutation testing como meta, não requisito hard (sem tooling no momento) | Deliberado | Baixa | Baixo | Não | Estável | Cobertura de linha (90%+) e branch (85%+) nos domínios principais já garante qualidade. O dep `mutmut` foi **removido**: a série 3.x quebra na inicialização (`copy_src_dir` varre a raiz do filesystem). Mutation testing fica sem ferramenta até pinar uma versão funcional, quando virar prioridade. [ADR-005](../arquitetura/adr/005-estrategia-testes.md) documenta a estratégia. |

### Segurança e Qualidade

Débitos relacionados a segurança e qualidade de código.

| # | Área | Descrição | Tipo | Severidade | Impacto no Negócio | Risco de Produção | Tendência de Crescimento | Justificativa |
|---|---|---|---|---|---|---|---|---|
| TD-013 | Testes | Sem testes BDD/Gherkin no MVP (pytest-bdd planejado) | Deliberado | Baixa | Baixo | Não | Estável | Testes E2E com Gherkin em português agregam rastreabilidade para requisitos, mas requerem feature files e steps adicionais. Prioridade para testes unitários e de integração no MVP. [ADR-013](../arquitetura/adr/013-testes-bdd-pytest-bdd.md). |
| TD-014 | Testes | Sem relatório visual de testes no MVP (nem Allure nem pytest-html configurados) | Deliberado | Baixa | Baixo | Não | Estável | Allure oferece relatórios visuais superiores, mas requer servidor dedicado. Nem Allure nem pytest-html estão configurados para relatório (pytest-html entra apenas transitivamente, via `pytest-selenium` do extra de UI, sem geração de relatório acionada). O débito é a ausência de relatório visual de testes; o gate atual reporta via cobertura/saída do pytest. |

## Herança para a fase 3

Ponto único de intake do que a fase 3 herda deste ledger — o que fica em aberto
por decisão, para ser reavaliado com o escopo da próxima fase:

- **TDs abertos (deliberados/aceitos):** TD-002 (histórico de orçamentos, RF-017 Could-Have), TD-004 (push/SMS), TD-006 (mutation testing sem tooling estável), TD-013 (BDD/Gherkin), TD-014 (relatório visual de testes). Detalhe e justificativa nos *Itens Abertos* acima.
- **Follow-ups de fechamento:** o plano de finalização da entrega ([finalization-plan.md](../entrega/fase2/finalization-plan.md)) é o rastreador dos follow-ups de fim de ciclo (ex.: semântica de rejeição do complementar, HMAC no Postman) — consultar lá o status vivo de cada um antes de reabrir.
- **Revisão de código pós-entrega (2026-07-03):** duas ondas de review (arquivo-a-arquivo + ponytail ultra, depois 18 perspectivas de design) aplicaram ~40 melhorias no branch de refactor e abriram issues no GitHub para o que exige design próprio ou não cabia perto da entrega. **Bugs** ([#163–#174](https://github.com/fiap-postech-sw-architecture/postech-sw-arch-p2/issues?q=is%3Aissue+is%3Aopen+label%3Abug)): resiliência do relay (#163–#166), sessão de auth (#167), LGPD (#168), UI (#169–#170/#174), HMAC do webhook (#171), CI/worktrees (#172), testes (#173). **Design/enhancement** ([#175–#184](https://github.com/fiap-postech-sw-architecture/postech-sw-arch-p2/issues?q=is%3Aissue+is%3Aopen+label%3Aenhancement)): rotação de chave de PII (#175), modelo de reserva no estoque (#176), contrato de erro unificado (#177), shadow-state dos mappings (#178), revogação de família de refresh (#179), acompanhamento sem PII na URL (#180), fronteira do shared kernel (#181), coluna `finalizada_em` (#182), auditoria LGPD durável (#183), gating do CD (#184).
- **Snapshots de campanha (não vivos):** [plano-ataque.md](plano-ataque.md) e [plano-acao-pos-auditoria.md](plano-acao-pos-auditoria.md) são históricos; as contagens e o status atual estão neste README.

## Considerações de Complexidade Algorítmica

Complexidade das operações principais do sistema.

| Operação | Complexidade | Estrutura | Justificativa |
|---|---|---|---|
| Bloqueio pessimista de estoque | O(n log n) ordenação + O(n) reserva | Array de `item_id` ordenado | Ordenação previne deadlocks ([ADR-008](../arquitetura/adr/008-bloqueio-pessimista-estoque.md)). Custo aceitável para n < 100 itens por OS. |
| Transição de status da OS | O(1) por transição | Lookup direto (dict/enum) | `MaquinaDeStatus` valida transição em tempo constante. |
| Busca de OS por placa | O(log n) | Índice B-tree PostgreSQL | Índice B-tree (unique) em `veiculos.placa`; a busca de OS resolve a placa pelo veículo (JOIN veículo→OS), logarítmica nessa resolução. |
| Cálculo de média de execução | O(n) | Agregação SQL | `AVG()` de `atualizado_em − criado_em` sobre OS em status final; full scan da tabela (o filtro de `status` não tem índice de suporte — os índices existentes são compostos `(cliente_id, status)`/`(veiculo_id, status)`, com `status` não-líder). Aceitável no volume do MVP. |
| Validação de CPF/CNPJ | O(1) | Cálculo aritmético | Dígitos verificadores calculados em tempo constante (brutils). |

A coluna `orcamento_json` já é `jsonb` nativo (TD-005, migração 004); um índice GIN só se justifica se surgir consulta por campo do orçamento — hoje ele é lido junto da OS, nunca filtrado. Demais otimizações (particionamento, índice de suporte ao `AVG` de média) a avaliar com dados reais de produção.

## Estratégia de Pagamento

1. **Boy Scout Rule**: cada alteração deixa o código melhor do que encontrou
2. **Refatorações incrementais**: melhorias técnicas nos sprints regulares, como parte do backlog
3. **Sprint técnico**: negociar com o PO para débitos de maior impacto
4. **ADRs como prevenção**: decisões registradas em ADR ([ADR-001](../arquitetura/adr/001-framework-fastapi.md) a [ADR-024](../arquitetura/adr/fase2/024-metricas-prometheus.md), incluindo os ADRs da fase 2 em `adr/fase2/`) evitam débitos invisíveis
5. **Métricas de fluxo**: lead time, cycle time e taxa de falhas para detectar crescimento do débito

> [↑ Raiz do projeto](../../README.md)
